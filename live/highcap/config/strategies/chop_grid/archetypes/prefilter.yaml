# Chop grid prefilter — see config/strategies/chop_grid/archetypes/prefilter.yaml (research).

rules:
  - all_of:
      - feature: box_pos_60
        operator: ">="
        value: 0.35
      - feature: box_pos_60
        operator: <=
        value: 0.65

# FER Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/fer/20260226_175746/strategies/fer/gate_draft.yaml
# 优化规则: 6/10 条通过优化, 8 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_fer_trapped_fer_trapped
  tag: PREFILTER_FER_TRAPPED_FER_TRAPPED
  phase: hard_gate
  priority: 1
  reason: 'prefilter OR: fer_trapped_longs_score>=3.4965 OR fer_trapped_shorts_score>=3.7941'
  when:
    all_of:
    - fer_trapped_longs_score:
        value_lt: 3.4965
    - fer_trapped_shorts_score:
        value_lt: 3.7941
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_cvd_divergence_score
  tag: HARD_CVD_DIVERGENCE_SCORE
  phase: hard_gate
  priority: 11
  reason: cvd_divergence_score 触发树分裂条件 (分裂 3 次)
  when:
    cvd_divergence_score:
      value_lt: -0.0306
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=-0.0306, lift=0.279'
- id: gate_garch_persistence
  tag: HARD_GARCH_PERSISTENCE
  phase: hard_gate
  priority: 10
  reason: garch_persistence 触发树分裂条件 (分裂 3 次)
  when:
    garch_persistence:
      value_gt: 0.479
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4790, lift=0.513'
guardrails: []

# BPC Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/bpc/20260308_204733/strategies/bpc/gate_draft.yaml
# 优化规则: 0/5 条通过优化, 5 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: prefilter_bpc_bb_compression
  tag: PREFILTER_BPC_BB_COMPRESSION
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_bb_compression > 0.9462'
  when:
    bpc_bb_compression:
      value_gt: 0.9462
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_bpc_volume_compression_pct
  tag: PREFILTER_BPC_VOLUME_COMPRESSION_PCT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_volume_compression_pct > 0.9239'
  when:
    bpc_volume_compression_pct:
      value_gt: 0.9239
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_bpc_impulse_return_atr
  tag: PREFILTER_BPC_IMPULSE_RETURN_ATR
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_impulse_return_atr < -0.9512'
  when:
    bpc_impulse_return_atr:
      value_lt: -0.9512
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
guardrails: []

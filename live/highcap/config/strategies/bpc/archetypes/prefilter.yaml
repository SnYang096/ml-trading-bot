# BPC Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-08
# 数据源: features_labeled.parquet, 40672 行

rules:
  - feature: bpc_bb_compression
    operator: ">"
    value: 0.9462
    rationale: "meta-algorithm, KS=0.1655, return_uplift=+0.0292, robustness=100%, holdout验证通过"
  - feature: bpc_volume_compression_pct
    operator: ">"
    value: 0.9239
    rationale: "meta-algorithm, KS=0.1448, return_uplift=-0.0937, robustness=100%, holdout验证通过"
  - feature: bpc_impulse_return_atr
    operator: "<"
    value: -0.9512
    rationale: "meta-algorithm, KS=0.1125, return_uplift=-0.0604, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-08) ──
  timestamp: "2026-03-08"
  data_source: "features_labeled.parquet"
  n_rows: 40672
  baseline_bad_rate: 0.0000
  baseline_median_rr: -0.0203

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


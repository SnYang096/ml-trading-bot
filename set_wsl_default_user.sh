#!/bin/bash
# 设置 WSL 默认用户为 yin
# 在 Windows PowerShell 或 CMD 中运行：
# ubuntu-22.04 config --default-user yin

echo "请在 Windows PowerShell 或 CMD 中运行以下命令："
echo ""
echo "  ubuntu-22.04 config --default-user yin"
echo ""
echo "或者使用 wsl 命令："
echo "  wsl -d ubuntu-22.04 -u yin"
echo ""
echo "这将设置 WSL 的默认用户为 yin，这样 Cursor 打开终端时就会使用 yin 用户而不是 root。"


# Default: no external receivers — Grafana starts without Telegram token.
# Enable Telegram: cp contact-points.telegram.yml.template → contact-points.telegram.yml
# and set GRAFANA_ALERT_TELEGRAM_BOT_TOKEN in monitoring/.env (see .env.example).
apiVersion: 1

contactPoints: []

apiVersion: 1

datasources:
  - name: Prometheus
    # Stable UID referenced by dashboard JSON panels (provisioned dashboards do not
    # expand ${DS_PROMETHEUS} automatically).
    uid: prometheus-monitoring
    type: prometheus
    access: proxy
    url: http://prometheus:9091
    isDefault: true
    editable: true

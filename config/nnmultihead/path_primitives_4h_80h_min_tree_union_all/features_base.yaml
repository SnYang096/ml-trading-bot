# nnmultihead Pool A（features_base）
# - 这些特征是 primitives label / Router slicing / 最小结构信息所必需的“底座”
# - 永远包含，不参与 feature-group-search 的候选筛选
#
# 注意：
# - `atr_f` 必须保留（用于 primitives labels 的 ATR 归一化尺度列 `atr`）
# - raw `atr` 是否喂给 MLP 由 selector 的 exclude_columns 控制（默认 nn 侧会排除）

- atr_f
- atr_percentile_f
- bb_width_f
- rsi_f
- wick_ratios_f
- range_ratio_5bar_f
- price_range_symmetry_f
- volume_anomaly_f



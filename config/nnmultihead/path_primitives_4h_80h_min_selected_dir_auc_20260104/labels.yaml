# Note: Strategy ID is the directory name (path_primitives_4h_80h_min_selected_dir_auc_20260104).
# The 'name' field is optional and only for display purposes.
target_column: dummy_label

# Note: For nnmultihead training, we do NOT use this label_generator.
# It exists only to satisfy the generic config loader schema.
label_generator:
  module: src.time_series_model.models.nn.path_primitives_labels
  function: compute_path_primitives_labels
  params: {}

filters: []
post_label_filters: []



# TPC regime 有符号斜率对照（可选；生产仍为 gate H + regime |pos|>=0.10）
#
#   H_prod     → config/strategies（或 H_bull_conditional_vol 实验树若要对齐 gate）
#   Fp_sym     → Fp_ema_plus_slope_strategies（|slope|>=0.002）
#   bull_trend → 新建 config_experiments/tpc_regime_bull_trend_strategies
#   bear_trend → 新建 config_experiments/tpc_regime_bear_trend_strategies
#
# 建树后跑:
#   PYTHONPATH=src:scripts python -m scripts.event_backtest \
#     --variant-grid config/experiments/tpc_regime_slope_signed_grid.yaml --quiet-signal-logs

experiment_id: tpc_regime_slope_signed_20260527
strategy: tpc
symbols:
  - BTCUSDT
  - ETHUSDT
  - SOLUSDT
  - BNBUSDT
  - XRPUSDT
  - ADAUSDT

runs: []
# 占位：先跑 rd_loop_tpc_regime_slope_signed.yaml 看 label/IC，再填 runs 与实验树。

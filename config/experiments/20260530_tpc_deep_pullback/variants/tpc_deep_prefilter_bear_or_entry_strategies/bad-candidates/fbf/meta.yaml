# FBF (FailedBreakoutFailure) 策略元信息

strategy:
  name: fbf-120T
  description: |
    FailedBreakoutFailure archetype: 关键边界突破失败后的反向清算。
    核心：边界（SR/区间）突破未站稳，反向交易失败推进一侧。

  owner: quant_team
  archetype: FailedBreakoutFailure

  timeframe: "120T"

  decision_alignment:
    enabled: true
    mode: prefilter_recent_window
    window_bars: 3
    layers_allow_recent_window: [prefilter]
    layers_required_same_bar: [direction, gate, entry_filter]

  macro_tp_vwap_anchor:
    enabled: false
    symbol: BTCUSDT

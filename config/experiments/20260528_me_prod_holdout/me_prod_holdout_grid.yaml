# ME prod E2E holdout — 当前 config/strategies/me archetypes 全栈验证
#
# 层: regime(chop) + prefilter v3 + direction v6 + gate + entry filters: []
# 对比: me_direction_v6_holdout_grid.yaml 用的是实验树，本 grid 用 prod。
#
# 跑法:
#   PYTHONPATH=src:scripts python -m scripts.event_backtest \
#     --variant-grid config/experiments/20260528_me_prod_holdout/me_prod_holdout_grid.yaml --quiet-signal-logs

experiment_id: me_prod_holdout_20260528
strategy: me
symbols:
  - BTCUSDT
  - ETHUSDT
  - BNBUSDT
  - ADAUSDT
  - SOLUSDT
  - XRPUSDT

runs:
  - variant: prod_holdout_full
    strategies_root: config/strategies
    start_date: "2024-07-01"
    end_date: "2026-05-01"
    output_dir: results/me/experiments/prod_holdout/full

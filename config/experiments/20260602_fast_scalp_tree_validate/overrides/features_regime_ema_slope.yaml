# Extra runtime features for G20 regime (EMA position + slope at event time).

name: fast_scalp_regime_ema_slope
description: EMA1200 macro position and 10-bar slope for regime side mask.

feature_pipeline:
  requested_features:
    - atr_f
    - macro_tp_vwap_1200_position
    - ema_1200_slope_f

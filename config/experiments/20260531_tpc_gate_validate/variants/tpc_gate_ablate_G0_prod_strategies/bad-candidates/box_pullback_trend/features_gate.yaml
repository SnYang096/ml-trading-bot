name: box_pullback_trend_gate
description: |
  Gate deny-feature whitelist for BPT research. Keep this narrow while the
  strategy is still being validated.

feature_pipeline:
  allowed_gate_deny_features:
    - "bpt_box_pullback_window"
    - "bpt_macro_box_direction"
    - "bpt_semantic_chop"
    - "box_*"
    - "ema_1200_position"
    - "ema_1200_slope_10"
    - "vol_persistence"
    - "bb_width_normalized_pct"

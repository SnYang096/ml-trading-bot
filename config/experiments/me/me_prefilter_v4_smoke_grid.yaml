# ME prefilter v4 SMOKE — 2 币 × 6 个月，快速漏斗验证（修了 entry_filter bug 后）
#
# 目的：在 entry_filter bug 修复后，用最快方式验证 v3 prefilter pass 的信号
# 在干净 PCM 路径下是否能落地成交。完整 6 币 × 2 年 holdout 留待 smoke OK 再跑。
#
# 跑法:
#   PYTHONPATH=src:scripts python -m scripts.event_backtest \
#     --variant-grid config/experiments/me/me_prefilter_v4_smoke_grid.yaml --quiet-signal-logs

experiment_id: me_prefilter_v4_smoke_20260528
strategy: me
symbols:
  - BTCUSDT
  - ETHUSDT

runs:
  - variant: v4_smoke_2024H2
    strategies_root: config_experiments/me_prefilter_fix_v4_no_safety_gates_strategies
    start_date: "2024-07-01"
    end_date: "2025-01-01"
    output_dir: results/me/experiments/prefilter_fix/v4_smoke_2024H2

# FER-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260327_193117_rr_extreme/fer-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-120T
#   python scripts/optimize_gate_unified.py --strategy fer-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_sma_200_position
  tag: HARD_SMA_200_POSITION
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.77)
  when:
    sma_200_position:
      value_lt: -0.0177
  then:
    action: deny
  comment: 'statistical(lift=1.77): sma_200_position < -0.01767'
- id: gate_spectrum_cvd_centroid_spectrum_cvd_centroid
  tag: HARD_SPECTRUM_CVD_CENTROID_SPECTRUM_CVD_CENTROID
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.64)
  when:
    all_of:
    - spectrum_cvd_centroid:
        value_gt: 0.1597
    - spectrum_cvd_centroid:
        value_lt: 0.3006
  then:
    action: deny
  comment: 'statistical(lift=1.64): spectrum_cvd_centroid > 0.1597 & spectrum_cvd_centroid < 0.3006'
- id: gate_hilbert_cvd_price_env_ratio
  tag: HARD_HILBERT_CVD_PRICE_ENV_RATIO
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.38)
  when:
    hilbert_cvd_price_env_ratio:
      value_gt: 0.9029
  then:
    action: deny
  comment: 'statistical(lift=1.38): hilbert_cvd_price_env_ratio > 0.9029'
- id: gate_bpc_pullback_depth_bpc_pullback_depth
  tag: HARD_BPC_PULLBACK_DEPTH_BPC_PULLBACK_DEPTH
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.28)
  when:
    all_of:
    - bpc_pullback_depth:
        value_gt: 0.6094
    - bpc_pullback_depth:
        value_lt: 0.7395
  then:
    action: deny
  comment: 'statistical(lift=1.28): bpc_pullback_depth > 0.6094 & bpc_pullback_depth < 0.7395'
- id: gate_oi_flow_zscore_oi_flow_zscore
  tag: HARD_OI_FLOW_ZSCORE_OI_FLOW_ZSCORE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.11)
  when:
    all_of:
    - oi_flow_zscore:
        value_gt: 0.2817
    - oi_flow_zscore:
        value_lt: 0.5012
  then:
    action: deny
  comment: 'statistical(lift=1.11): oi_flow_zscore > 0.2817 & oi_flow_zscore < 0.5012'
guardrails: []

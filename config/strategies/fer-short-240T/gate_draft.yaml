# FER-SHORT Gate Draft (auto-generated)
# 来源: results/train_final_20260316_070057_rr_extreme/fer-short
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short
#   python scripts/optimize_gate_unified.py --strategy fer-short --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_wpt_energy_cascade
  tag: HARD_WPT_ENERGY_CASCADE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.48)
  when:
    wpt_energy_cascade:
      value_gt: 0.4012
  then:
    action: deny
  comment: 'statistical(lift=1.48): wpt_energy_cascade > 0.4012'
- id: gate_oi_exhaustion_score_oi_exhaustion_score
  tag: HARD_OI_EXHAUSTION_SCORE_OI_EXHAUSTION_SCORE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - oi_exhaustion_score:
        value_gt: 0.0073
    - oi_exhaustion_score:
        value_lt: 0.046
  then:
    action: deny
  comment: 'statistical(lift=1.08): oi_exhaustion_score > 0.007305 & oi_exhaustion_score < 0.04598'
- id: gate_vol_zscore_vol_zscore
  tag: HARD_VOL_ZSCORE_VOL_ZSCORE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.43)
  when:
    all_of:
    - vol_zscore:
        value_gt: -1.098
    - vol_zscore:
        value_lt: -0.7155
  then:
    action: deny
  comment: 'statistical(lift=1.43): vol_zscore > -1.098 & vol_zscore < -0.7155'
- id: gate_hilbert_cvd_price_env_ratio_hilbert_cvd_price_env_ratio
  tag: HARD_HILBERT_CVD_PRICE_ENV_RATIO_HILBERT_CVD_PRICE_ENV_RATIO
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.13)
  when:
    all_of:
    - hilbert_cvd_price_env_ratio:
        value_gt: -0.3633
    - hilbert_cvd_price_env_ratio:
        value_lt: -0.2691
  then:
    action: deny
  comment: 'statistical(lift=1.13): hilbert_cvd_price_env_ratio > -0.3633 & hilbert_cvd_price_env_ratio < -0.2691'
guardrails: []

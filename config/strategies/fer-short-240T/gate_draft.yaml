# FER-SHORT-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260320_041721_rr_extreme/fer-short-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-240T
#   python scripts/optimize_gate_unified.py --strategy fer-short-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_tail_shape_right
  tag: HARD_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=2.00)
  when:
    evt_tail_shape_right:
      value_lt: 0.077
  then:
    action: deny
  comment: 'statistical(lift=2.00): evt_tail_shape_right < 0.07698'
- id: gate_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.46)
  when:
    fer_trapped_shorts_score:
      value_lt: 0.1852
  then:
    action: deny
  comment: 'statistical(lift=1.46): fer_trapped_shorts_score < 0.1852'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.64)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.1325
    - vol_leverage_asymmetry:
        value_lt: 0.1681
  then:
    action: deny
  comment: 'statistical(lift=1.64): vol_leverage_asymmetry > 0.1325 & vol_leverage_asymmetry < 0.1681'
- id: gate_evt_var_99_left_evt_var_99_left
  tag: HARD_EVT_VAR_99_LEFT_EVT_VAR_99_LEFT
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.96)
  when:
    all_of:
    - evt_var_99_left:
        value_gt: 0.1627
    - evt_var_99_left:
        value_lt: 0.2659
  then:
    action: deny
  comment: 'statistical(lift=1.96): evt_var_99_left > 0.1627 & evt_var_99_left < 0.2659'
- id: gate_vol_persistence_vol_persistence
  tag: HARD_VOL_PERSISTENCE_VOL_PERSISTENCE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.76)
  when:
    all_of:
    - vol_persistence:
        value_gt: 0.1671
    - vol_persistence:
        value_lt: 0.2119
  then:
    action: deny
  comment: 'statistical(lift=1.76): vol_persistence > 0.1671 & vol_persistence < 0.2119'
guardrails: []

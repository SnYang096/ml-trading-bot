# FER-SHORT-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260323_145206_rr_extreme/fer-short-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-240T
#   python scripts/optimize_gate_unified.py --strategy fer-short-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.32)
  when:
    vol_leverage_asymmetry:
      value_lt: 0.0345
  then:
    action: deny
  comment: 'statistical(lift=1.32): vol_leverage_asymmetry < 0.03446'
- id: gate_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.30)
  when:
    fer_trapped_shorts_score:
      value_gt: 0.9373
  then:
    action: deny
  comment: 'statistical(lift=1.30): fer_trapped_shorts_score > 0.9373'
- id: gate_vol_clustering_strength
  tag: HARD_VOL_CLUSTERING_STRENGTH
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.16)
  when:
    vol_clustering_strength:
      value_gt: 0.216
  then:
    action: deny
  comment: 'statistical(lift=1.16): vol_clustering_strength > 0.216'
- id: gate_vol_persistence_vol_persistence
  tag: HARD_VOL_PERSISTENCE_VOL_PERSISTENCE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.18)
  when:
    all_of:
    - vol_persistence:
        value_gt: 0.0269
    - vol_persistence:
        value_lt: 0.0648
  then:
    action: deny
  comment: 'statistical(lift=1.18): vol_persistence > 0.02692 & vol_persistence < 0.06478'
- id: gate_cvd_change_5_normalized
  tag: HARD_CVD_CHANGE_5_NORMALIZED
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.11)
  when:
    cvd_change_5_normalized:
      value_lt: -0.0349
  then:
    action: deny
  comment: 'statistical(lift=1.11): cvd_change_5_normalized < -0.03493'
guardrails: []

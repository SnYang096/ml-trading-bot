# ME-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260327_193328_rr_extreme/me-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-short-120T
#   python scripts/optimize_gate_unified.py --strategy me-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_fer_signed_efficiency_pct
  tag: HARD_FER_SIGNED_EFFICIENCY_PCT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.57)
  when:
    fer_signed_efficiency_pct:
      value_lt: 0.125
  then:
    action: deny
  comment: 'statistical(lift=1.57): fer_signed_efficiency_pct < 0.125'
- id: gate_bpc_recovery_strength
  tag: HARD_BPC_RECOVERY_STRENGTH
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.09)
  when:
    bpc_recovery_strength:
      value_gt: 0.5478
  then:
    action: deny
  comment: 'statistical(lift=1.09): bpc_recovery_strength > 0.5478'
- id: gate_roc_10_roc_10
  tag: HARD_ROC_10_ROC_10
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.31)
  when:
    all_of:
    - roc_10:
        value_gt: 1.3185
    - roc_10:
        value_lt: 4.3652
  then:
    action: deny
  comment: 'statistical(lift=1.31): roc_10 > 1.319 & roc_10 < 4.365'
- id: gate_me_vol_divergence
  tag: HARD_ME_VOL_DIVERGENCE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.08)
  when:
    me_vol_divergence:
      value_lt: 0.1283
  then:
    action: deny
  comment: 'statistical(lift=1.08): me_vol_divergence < 0.1283'
- id: gate_vpin_ignition_score_vpin_ignition_score
  tag: HARD_VPIN_IGNITION_SCORE_VPIN_IGNITION_SCORE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.19)
  when:
    all_of:
    - vpin_ignition_score:
        value_gt: 0.0187
    - vpin_ignition_score:
        value_lt: 0.0426
  then:
    action: deny
  comment: 'statistical(lift=1.19): vpin_ignition_score > 0.0187 & vpin_ignition_score < 0.04258'
guardrails: []

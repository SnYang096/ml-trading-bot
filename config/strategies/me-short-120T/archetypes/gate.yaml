# ME-SHORT-120T Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/prod_train_history/me-short-120T/20260324_210653/strategies/me-short-120T/gate_draft.yaml
# 优化规则: 0/5 条通过优化, 3 条已移除
# Phase split: system_safety=1, hard_gate=1

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vpin_ma20_vpin_ma20
  tag: HARD_VPIN_MA20_VPIN_MA20
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.21)
  when:
    all_of:
    - vpin_ma20:
        value_gt: 0.3048
    - vpin_ma20:
        value_lt: 0.3202
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=0.2671, lift=0.452'
guardrails: []
system_safety:
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: system_safety
  priority: 12
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.0558
    - vol_leverage_asymmetry:
        value_lt: 0.1482
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=0.0509, lift=0.123'

# BPC-SHORT Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-14
# 数据源: features_labeled.parquet, 27840 行

rules:
  - feature: bpc_volume_compression_pct
    operator: ">="
    value: 0.1466
    rationale: "meta-algorithm, KS=0.1608, return_uplift=+1.1365, robustness=100%, holdout验证通过"
  - feature: shd_pct
    operator: ">="
    value: 0.8497
    rationale: "meta-algorithm, KS=0.1171, return_uplift=+1.0761, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-14) ──
  timestamp: "2026-03-14"
  data_source: "features_labeled.parquet"
  n_rows: 27840
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.1630

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


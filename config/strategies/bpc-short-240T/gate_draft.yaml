# BPC-SHORT-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260323_145230_rr_extreme/bpc-short-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-short-240T
#   python scripts/optimize_gate_unified.py --strategy bpc-short-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.39)
  when:
    vol_leverage_asymmetry:
      value_lt: 0.0318
  then:
    action: deny
  comment: 'statistical(lift=1.39): vol_leverage_asymmetry < 0.03184'
- id: gate_fer_efficiency_flip
  tag: HARD_FER_EFFICIENCY_FLIP
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.11)
  when:
    fer_efficiency_flip:
      value_lt: 0.0078
  then:
    action: deny
  comment: 'statistical(lift=1.11): fer_efficiency_flip < 0.007831'
- id: gate_me_atr_pct
  tag: HARD_ME_ATR_PCT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.11)
  when:
    me_atr_pct:
      value_gt: 0.92
  then:
    action: deny
  comment: 'statistical(lift=1.11): me_atr_pct > 0.92'
- id: gate_fer_signed_efficiency_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.07)
  when:
    all_of:
    - fer_signed_efficiency:
        value_gt: 0.0
    - fer_signed_efficiency:
        value_lt: 0.001
  then:
    action: deny
  comment: 'statistical(lift=1.07): fer_signed_efficiency > 7.351e-06 & fer_signed_efficiency < 0.0009592'
- id: gate_me_vol_regime_me_vol_regime
  tag: HARD_ME_VOL_REGIME_ME_VOL_REGIME
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.05)
  when:
    all_of:
    - me_vol_regime:
        value_gt: 0.51
    - me_vol_regime:
        value_lt: 0.77
  then:
    action: deny
  comment: 'statistical(lift=1.05): me_vol_regime > 0.51 & me_vol_regime < 0.77'
guardrails: []

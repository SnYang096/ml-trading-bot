# ME-SHORT-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260318_225614_rr_extreme/me-short-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-short-240T
#   python scripts/optimize_gate_unified.py --strategy me-short-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_fer_momentum_efficiency_decay
  tag: HARD_FER_MOMENTUM_EFFICIENCY_DECAY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.60)
  when:
    fer_momentum_efficiency_decay:
      value_gt: 0.6954
  then:
    action: deny
  comment: 'statistical(lift=1.60): fer_momentum_efficiency_decay > 0.6954'
- id: gate_vp_width_ratio_vp_width_ratio
  tag: HARD_VP_WIDTH_RATIO_VP_WIDTH_RATIO
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.80)
  when:
    all_of:
    - vp_width_ratio:
        value_gt: 0.5428
    - vp_width_ratio:
        value_lt: 0.563
  then:
    action: deny
  comment: 'statistical(lift=1.80): vp_width_ratio > 0.5428 & vp_width_ratio < 0.563'
- id: gate_vp_poc_deviation
  tag: HARD_VP_POC_DEVIATION
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.50)
  when:
    vp_poc_deviation:
      value_lt: -0.2519
  then:
    action: deny
  comment: 'statistical(lift=1.50): vp_poc_deviation < -0.2519'
- id: gate_evt_var_99_right_evt_var_99_right
  tag: HARD_EVT_VAR_99_RIGHT_EVT_VAR_99_RIGHT
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.43)
  when:
    all_of:
    - evt_var_99_right:
        value_gt: 0.2917
    - evt_var_99_right:
        value_lt: 0.4563
  then:
    action: deny
  comment: 'statistical(lift=1.43): evt_var_99_right > 0.2917 & evt_var_99_right < 0.4563'
- id: gate_vp_entropy_vp_entropy
  tag: HARD_VP_ENTROPY_VP_ENTROPY
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.42)
  when:
    all_of:
    - vp_entropy:
        value_gt: 0.8735
    - vp_entropy:
        value_lt: 0.894
  then:
    action: deny
  comment: 'statistical(lift=1.42): vp_entropy > 0.8735 & vp_entropy < 0.894'
guardrails: []

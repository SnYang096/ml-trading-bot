# ME-SHORT-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260321_015532_rr_extreme/me-short-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-short-240T
#   python scripts/optimize_gate_unified.py --strategy me-short-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vpin_ma10
  tag: HARD_VPIN_MA10
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.44)
  when:
    vpin_ma10:
      value_gt: 0.3243
  then:
    action: deny
  comment: 'statistical(lift=1.44): vpin_ma10 > 0.3243'
- id: gate_vpin_max5
  tag: HARD_VPIN_MAX5
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.20)
  when:
    vpin_max5:
      value_gt: 0.4467
  then:
    action: deny
  comment: 'statistical(lift=1.20): vpin_max5 > 0.4467'
- id: gate_fer_efficiency_flip
  tag: HARD_FER_EFFICIENCY_FLIP
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.12)
  when:
    fer_efficiency_flip:
      value_lt: 0.0008
  then:
    action: deny
  comment: 'statistical(lift=1.12): fer_efficiency_flip < 0.0007585'
- id: gate_wpt_multi_scale_consistency
  tag: HARD_WPT_MULTI_SCALE_CONSISTENCY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.10)
  when:
    wpt_multi_scale_consistency:
      value_gt: 0.6
  then:
    action: deny
  comment: 'statistical(lift=1.10): wpt_multi_scale_consistency > 0.6'
- id: gate_wpt_breakout_confidence_wpt_breakout_confidence
  tag: HARD_WPT_BREAKOUT_CONFIDENCE_WPT_BREAKOUT_CONFIDENCE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.10)
  when:
    all_of:
    - wpt_breakout_confidence:
        value_gt: 0.528
    - wpt_breakout_confidence:
        value_lt: 0.557
  then:
    action: deny
  comment: 'statistical(lift=1.10): wpt_breakout_confidence > 0.528 & wpt_breakout_confidence < 0.557'
guardrails: []

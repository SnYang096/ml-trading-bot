strategy:
  name: sr_reversal_rr_reg_long_trade_cluster_semantic
  description: >
    TradeCluster semantics only (exhaustion vs absorption) for reversals.
    Avoids feeding raw trade_cluster_* stats into the model.
  tags:
    - sr
    - reversal
    - intraday
    - long_only
    - regression
    - order_flow
    - trade_cluster
    - semantic
  owner: quant_team



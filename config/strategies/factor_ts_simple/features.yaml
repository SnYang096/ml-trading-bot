name: factor_ts_simple
description: >
  Lightweight feature pipeline for single-factor evaluation.

feature_pipeline:
  requested_features:
    - atr
    - rsi
  post_processors: []
  ensure_signal_column:
    name: signal
    default_value: 0


# ME-Short Gate (待训练)
# 训练目标: failure_rr_extreme（做空踩大坑）= forward_rr_short < -0.8R
# 使用全量数据训练，不依赖 prefilter 子集

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates: []
guardrails: []

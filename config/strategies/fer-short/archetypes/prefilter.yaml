# FER-SHORT Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-15
# 数据源: features_labeled.parquet, 27840 行

rules:
  - feature: fer_signed_efficiency
    operator: ">="
    value: -0.0001802
    rationale: "range lower, meta-algorithm, KS=0.1679, return_uplift=+1.7561, robustness=100%, holdout验证通过"
  - feature: fer_signed_efficiency
    operator: "<="
    value: -5.939e-10
    rationale: "range upper, meta-algorithm, KS=0.1679, return_uplift=+1.7561, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-15) ──
  timestamp: "2026-03-15"
  data_source: "features_labeled.parquet"
  n_rows: 27840
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.1630

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


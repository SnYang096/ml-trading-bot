# FER-SHORT Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-14
# 数据源: features_labeled.parquet, 27840 行

rules:
  - feature: oi_trend_divergence_score
    operator: ">="
    value: 0.0003112
    rationale: "range lower, meta-algorithm, KS=0.1586, return_uplift=+3.0650, robustness=100%, holdout验证通过"
  - feature: oi_trend_divergence_score
    operator: "<="
    value: 0.3381
    rationale: "range upper, meta-algorithm, KS=0.1586, return_uplift=+3.0650, robustness=100%, holdout验证通过"
  - feature: vp_width_ratio
    operator: ">="
    value: 0.422
    rationale: "range lower, meta-algorithm, KS=0.1242, return_uplift=+2.7814, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-14) ──
  timestamp: "2026-03-14"
  data_source: "features_labeled.parquet"
  n_rows: 27840
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.1630

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


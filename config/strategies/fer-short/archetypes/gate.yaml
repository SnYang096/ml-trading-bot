# FER-SHORT Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/fer-short/20260314_213450/strategies/fer-short/gate_draft.yaml
# 优化规则: 2/5 条通过优化, 2 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_tail_shape_right
  tag: HARD_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.70)
  when:
    evt_tail_shape_right:
      value_lt: 0.604
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6040, lift=0.198'
- id: gate_evt_var_99_evt_var_99
  tag: HARD_EVT_VAR_99_EVT_VAR_99
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.23)
  when:
    all_of:
    - evt_var_99:
        value_gt: 0.7976
    - evt_var_99:
        value_lt: 0.8968
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4698, lift=0.230'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.25)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.1537
    - vol_leverage_asymmetry:
        value_lt: 0.2018
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.2606, lift=0.345'
guardrails: []

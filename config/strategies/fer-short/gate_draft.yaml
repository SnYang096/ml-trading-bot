# FER-SHORT Gate Draft (auto-generated)
# 来源: results/train_final_20260316_005011_rr_extreme/fer-short
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short
#   python scripts/optimize_gate_unified.py --strategy fer-short --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_atr_percentile
  tag: HARD_ATR_PERCENTILE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.46)
  when:
    atr_percentile:
      value_lt: 0.2037
  then:
    action: deny
  comment: 'statistical(lift=1.46): atr_percentile < 0.2037'
- id: gate_shd_pct_shd_pct
  tag: HARD_SHD_PCT_SHD_PCT
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.13)
  when:
    all_of:
    - shd_pct:
        value_gt: 0.3729
    - shd_pct:
        value_lt: 0.705
  then:
    action: deny
  comment: 'statistical(lift=1.13): shd_pct > 0.3729 & shd_pct < 0.705'
- id: gate_bpc_pullback_quality_bpc_pullback_quality
  tag: HARD_BPC_PULLBACK_QUALITY_BPC_PULLBACK_QUALITY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.19)
  when:
    all_of:
    - bpc_pullback_quality:
        value_gt: 0.2569
    - bpc_pullback_quality:
        value_lt: 0.3694
  then:
    action: deny
  comment: 'statistical(lift=1.19): bpc_pullback_quality > 0.2569 & bpc_pullback_quality < 0.3694'
- id: gate_fp_max_imbalance_ratio_fp_max_imbalance_ratio
  tag: HARD_FP_MAX_IMBALANCE_RATIO_FP_MAX_IMBALANCE_RATIO
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.13)
  when:
    all_of:
    - fp_max_imbalance_ratio:
        value_gt: 2.6795
    - fp_max_imbalance_ratio:
        value_lt: 3.2673
  then:
    action: deny
  comment: 'statistical(lift=1.13): fp_max_imbalance_ratio > 2.679 & fp_max_imbalance_ratio < 3.267'
- id: gate_spectrum_price_entropy_spectrum_price_entropy
  tag: HARD_SPECTRUM_PRICE_ENTROPY_SPECTRUM_PRICE_ENTROPY
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.09)
  when:
    all_of:
    - spectrum_price_entropy:
        value_gt: 0.9187
    - spectrum_price_entropy:
        value_lt: 0.9256
  then:
    action: deny
  comment: 'statistical(lift=1.09): spectrum_price_entropy > 0.9187 & spectrum_price_entropy < 0.9256'
guardrails: []

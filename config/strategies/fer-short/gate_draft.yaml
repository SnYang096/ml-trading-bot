# FER-SHORT Gate Draft (auto-generated)
# 来源: results/train_final_20260315_203556_rr_extreme/fer-short
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short
#   python scripts/optimize_gate_unified.py --strategy fer-short --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_tail_shape_right
  tag: HARD_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.72)
  when:
    evt_tail_shape_right:
      value_lt: 0.1151
  then:
    action: deny
  comment: 'statistical(lift=1.72): evt_tail_shape_right < 0.1151'
- id: gate_wpt_breakout_confidence
  tag: HARD_WPT_BREAKOUT_CONFIDENCE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.64)
  when:
    wpt_breakout_confidence:
      value_gt: 0.5868
  then:
    action: deny
  comment: 'statistical(lift=1.64): wpt_breakout_confidence > 0.5868'
- id: gate_hilbert_cvd_price_env_ratio
  tag: HARD_HILBERT_CVD_PRICE_ENV_RATIO
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.08)
  when:
    hilbert_cvd_price_env_ratio:
      value_gt: -0.4301
  then:
    action: deny
  comment: 'statistical(lift=1.08): hilbert_cvd_price_env_ratio > -0.4301'
- id: gate_volume_stability_volume_stability
  tag: HARD_VOLUME_STABILITY_VOLUME_STABILITY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.23)
  when:
    all_of:
    - volume_stability:
        value_gt: 0.356
    - volume_stability:
        value_lt: 0.4397
  then:
    action: deny
  comment: 'statistical(lift=1.23): volume_stability > 0.356 & volume_stability < 0.4397'
- id: gate_session_id
  tag: HARD_SESSION_ID
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.13)
  when:
    session_id:
      value_gt: 1.0
  then:
    action: deny
  comment: 'statistical(lift=1.13): session_id > 1'
guardrails: []

# FER-SHORT Gate Draft (auto-generated)
# 来源: results/train_final_20260315_172051_rr_extreme/fer-short
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short
#   python scripts/optimize_gate_unified.py --strategy fer-short --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_var_99
  tag: HARD_EVT_VAR_99
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.80)
  when:
    evt_var_99:
      value_gt: 0.7738
  then:
    action: deny
  comment: 'statistical(lift=1.80): evt_var_99 > 0.7738'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=2.02)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.1563
    - vol_leverage_asymmetry:
        value_lt: 0.1813
  then:
    action: deny
  comment: 'statistical(lift=2.02): vol_leverage_asymmetry > 0.1563 & vol_leverage_asymmetry < 0.1813'
- id: gate_fer_absorption_streak_fer_absorption_streak
  tag: HARD_FER_ABSORPTION_STREAK_FER_ABSORPTION_STREAK
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.13)
  when:
    all_of:
    - fer_absorption_streak:
        value_gt: 0.35
    - fer_absorption_streak:
        value_lt: 0.81
  then:
    action: deny
  comment: 'statistical(lift=1.13): fer_absorption_streak > 0.35 & fer_absorption_streak < 0.81'
- id: gate_evt_tail_shape_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.38)
  when:
    all_of:
    - evt_tail_shape:
        value_gt: 0.429
    - evt_tail_shape:
        value_lt: 0.6429
  then:
    action: deny
  comment: 'statistical(lift=1.38): evt_tail_shape > 0.429 & evt_tail_shape < 0.6429'
- id: gate_evt_scale_right_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.26)
  when:
    all_of:
    - evt_scale_right:
        value_gt: 0.3335
    - evt_scale_right:
        value_lt: 0.5317
  then:
    action: deny
  comment: 'statistical(lift=1.26): evt_scale_right > 0.3335 & evt_scale_right < 0.5317'
guardrails: []

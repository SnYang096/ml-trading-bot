# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260305_151332_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_scale_sma_200_position
  tag: HARD_EVT_SCALE_SMA_200_POSITION
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=2.02)
  when:
    all_of:
    - evt_scale:
        value_lt: 0.348
    - sma_200_position:
        value_gt: 0.0214
  then:
    action: deny
  comment: 'statistical(lift=2.02): evt_scale < 0.348 & sma_200_position > 0.02138'
- id: gate_macd_signal_atr
  tag: HARD_MACD_SIGNAL_ATR
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.45)
  when:
    macd_signal_atr:
      value_gt: 0.5405
  then:
    action: deny
  comment: 'statistical(lift=1.45): macd_signal_atr > 0.5405'
- id: gate_macd_atr_evt_tail_shape_right
  tag: HARD_MACD_ATR_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.93)
  when:
    all_of:
    - macd_atr:
        value_gt: 0.7666
    - evt_tail_shape_right:
        value_gt: 0.0476
  then:
    action: deny
  comment: 'statistical(lift=1.93): macd_atr > 0.7666 & evt_tail_shape_right > 0.04762'
- id: gate_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.09)
  when:
    evt_scale_right:
      value_gt: 0.8016
  then:
    action: deny
  comment: 'statistical(lift=1.09): evt_scale_right > 0.8016'
guardrails: []

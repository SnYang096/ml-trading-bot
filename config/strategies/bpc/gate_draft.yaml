# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260412_132938_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_macro_tp_vwap_1200_position
  tag: HARD_MACRO_TP_VWAP_1200_POSITION
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.36)
  when:
    macro_tp_vwap_1200_position:
      value_lt: -0.4758
  then:
    action: deny
  comment: 'statistical(lift=1.36): macro_tp_vwap_1200_position < -0.4758'
- id: gate_ema_1200_position
  tag: HARD_EMA_1200_POSITION
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.07)
  when:
    ema_1200_position:
      value_lt: -0.4787
  then:
    action: deny
  comment: 'statistical(lift=1.07): ema_1200_position < -0.4787'
- id: gate_cvd_change_5_normalized_cvd_change_5_normalized
  tag: HARD_CVD_CHANGE_5_NORMALIZED_CVD_CHANGE_5_NORMALIZED
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.09)
  when:
    all_of:
    - cvd_change_5_normalized:
        value_gt: -0.037
    - cvd_change_5_normalized:
        value_lt: -0.0258
  then:
    action: deny
  comment: 'statistical(lift=1.09): cvd_change_5_normalized > -0.03695 & cvd_change_5_normalized < -0.02576'
guardrails: []

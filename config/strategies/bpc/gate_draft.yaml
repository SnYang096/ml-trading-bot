# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260424_032558_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_ema_1200_position
  tag: HARD_EMA_1200_POSITION
  phase: hard_gate
  priority: 10
  reason: ema_1200_position 触发树分裂条件 (分裂 2 次)
  when:
    ema_1200_position:
      value_lt: -0.0377
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 -0.03767 | 方向: data_verified (low=0.982, high=5.195)'
- id: gate_macro_tp_vwap_1200_position
  tag: HARD_MACRO_TP_VWAP_1200_POSITION
  phase: hard_gate
  priority: 11
  reason: macro_tp_vwap_1200_position 触发树分裂条件 (分裂 1 次)
  when:
    macro_tp_vwap_1200_position:
      value_gt: 0.1512
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.1512 | 方向: data_verified (low=1.461, high=0.000)'
guardrails: []

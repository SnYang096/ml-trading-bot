# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260410_130538_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_macro_tp_vwap_1200_position_macro_tp_vwap_1200_position
  tag: HARD_MACRO_TP_VWAP_1200_POSITION_MACRO_TP_VWAP_1200_POSITION
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.16)
  when:
    all_of:
    - macro_tp_vwap_1200_position:
        value_gt: -0.1256
    - macro_tp_vwap_1200_position:
        value_lt: 0.1575
  then:
    action: deny
  comment: 'statistical(lift=1.16): macro_tp_vwap_1200_position > -0.1256 & macro_tp_vwap_1200_position < 0.1575'
- id: gate_evt_scale_evt_scale
  tag: HARD_EVT_SCALE_EVT_SCALE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.15)
  when:
    all_of:
    - evt_scale:
        value_gt: 0.246
    - evt_scale:
        value_lt: 0.5238
  then:
    action: deny
  comment: 'statistical(lift=1.15): evt_scale > 0.246 & evt_scale < 0.5238'
- id: gate_fer_signed_efficiency_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.09)
  when:
    all_of:
    - fer_signed_efficiency:
        value_gt: 0.0
    - fer_signed_efficiency:
        value_lt: 0.0005
  then:
    action: deny
  comment: 'statistical(lift=1.09): fer_signed_efficiency > 2.189e-09 & fer_signed_efficiency < 0.0004633'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.0558
    - vol_leverage_asymmetry:
        value_lt: 0.1482
  then:
    action: deny
  comment: 'statistical(lift=1.08): vol_leverage_asymmetry > 0.05582 & vol_leverage_asymmetry < 0.1482'
- id: gate_evt_es_99_evt_es_99
  tag: HARD_EVT_ES_99_EVT_ES_99
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.12)
  when:
    all_of:
    - evt_es_99:
        value_gt: 0.3532
    - evt_es_99:
        value_lt: 0.5119
  then:
    action: deny
  comment: 'statistical(lift=1.12): evt_es_99 > 0.3532 & evt_es_99 < 0.5119'
guardrails: []

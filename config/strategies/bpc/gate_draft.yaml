# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260223_234340_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: gate_macd_signal_atr
  tag: HARD_MACD_SIGNAL_ATR
  phase: hard_gate
  priority: 10
  reason: macd_signal_atr 触发树分裂条件 (分裂 1 次)
  when:
    macd_signal_atr:
      value_lt: -0.7204
  then:
    action: deny
  comment: '自动生成: 分裂 1 次 | 阈值 -0.7204 | 方向: data_verified (low=-3.120, high=-0.117)'
- id: gate_atr_percentile
  tag: HARD_ATR_PERCENTILE
  phase: hard_gate
  priority: 11
  reason: atr_percentile 触发树分裂条件 (分裂 1 次)
  when:
    atr_percentile:
      value_lt: 0.5889
  then:
    action: deny
  comment: '自动生成: 分裂 1 次 | 阈值 0.5889 | 方向: data_verified (low=-1.963, high=1.237)'
- id: gate_oi_compression_score
  tag: HARD_OI_COMPRESSION_SCORE
  phase: hard_gate
  priority: 12
  reason: oi_compression_score 触发树分裂条件 (分裂 1 次)
  when:
    oi_compression_score:
      value_lt: 0.2729
  then:
    action: deny
  comment: '自动生成: 分裂 1 次 | 阈值 0.2729 | 方向: data_verified (low=-0.574, high=-0.299)'
- id: gate_garch_persistence
  tag: HARD_GARCH_PERSISTENCE
  phase: hard_gate
  priority: 13
  reason: garch_persistence 触发树分裂条件 (分裂 1 次)
  when:
    garch_persistence:
      value_lt: 0.8995
  then:
    action: deny
  comment: '自动生成: 分裂 1 次 | 阈值 0.8995 | 方向: data_verified (low=-1.672, high=0.768)'
guardrails: []

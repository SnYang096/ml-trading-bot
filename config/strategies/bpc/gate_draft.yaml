# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260308_204819_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.46)
  when:
    fer_signed_efficiency:
      value_lt: 0.0
  then:
    action: deny
  comment: 'statistical(lift=1.46): fer_signed_efficiency < 0'
- id: gate_macd_atr_macd_atr
  tag: HARD_MACD_ATR_MACD_ATR
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.68)
  when:
    all_of:
    - macd_atr:
        value_gt: 0.3237
    - macd_atr:
        value_lt: 0.676
  then:
    action: deny
  comment: 'statistical(lift=1.68): macd_atr > 0.3237 & macd_atr < 0.676'
- id: gate_macd_signal_atr_macd_signal_atr
  tag: HARD_MACD_SIGNAL_ATR_MACD_SIGNAL_ATR
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.64)
  when:
    all_of:
    - macd_signal_atr:
        value_gt: 0.2945
    - macd_signal_atr:
        value_lt: 0.6335
  then:
    action: deny
  comment: 'statistical(lift=1.64): macd_signal_atr > 0.2945 & macd_signal_atr < 0.6335'
- id: gate_macd_histogram_atr_macd_histogram_atr
  tag: HARD_MACD_HISTOGRAM_ATR_MACD_HISTOGRAM_ATR
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.32)
  when:
    all_of:
    - macd_histogram_atr:
        value_gt: -0.163
    - macd_histogram_atr:
        value_lt: -0.0946
  then:
    action: deny
  comment: 'statistical(lift=1.32): macd_histogram_atr > -0.163 & macd_histogram_atr < -0.09462'
- id: gate_fer_aggressor_absorption_fer_aggressor_absorption
  tag: HARD_FER_AGGRESSOR_ABSORPTION_FER_AGGRESSOR_ABSORPTION
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.20)
  when:
    all_of:
    - fer_aggressor_absorption:
        value_gt: 0.0
    - fer_aggressor_absorption:
        value_lt: 0.0
  then:
    action: deny
  comment: 'statistical(lift=1.20): fer_aggressor_absorption > 5.885e-11 & fer_aggressor_absorption < 4e-06'
guardrails: []

# BPC Gate Draft (auto-generated)
# 来源: results/train_final_20260304_222546_rr_extreme/bpc
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc
#   python scripts/optimize_gate_unified.py --strategy bpc --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: gate_evt_es_99_right
  tag: HARD_EVT_ES_99_RIGHT
  phase: hard_gate
  priority: 10
  reason: evt_es_99_right 触发树分裂条件 (分裂 2 次)
  when:
    evt_es_99_right:
      value_gt: 0.7202
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.7202 | 方向: data_verified (low=-0.375, high=-0.997)'
- id: gate_shd_pct
  tag: HARD_SHD_PCT
  phase: hard_gate
  priority: 11
  reason: shd_pct 触发树分裂条件 (分裂 2 次)
  when:
    shd_pct:
      value_lt: 0.4443
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.4443 | 方向: data_verified (low=-1.055, high=-0.214)'
- id: gate_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 12
  reason: evt_scale_right 触发树分裂条件 (分裂 2 次)
  when:
    evt_scale_right:
      value_gt: 0.2401
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.2401 | 方向: data_verified (low=0.097, high=-0.854)'
- id: gate_macd_atr
  tag: HARD_MACD_ATR
  phase: hard_gate
  priority: 13
  reason: macd_atr 触发树分裂条件 (分裂 2 次)
  when:
    macd_atr:
      value_lt: -0.9835
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 -0.9835 | 方向: data_verified (low=-1.039, high=-0.548)'
- id: gate_spectrum_volume_low_freq_ratio
  tag: HARD_SPECTRUM_VOLUME_LOW_FREQ_RATIO
  phase: hard_gate
  priority: 14
  reason: spectrum_volume_low_freq_ratio 触发树分裂条件 (分裂 2 次)
  when:
    spectrum_volume_low_freq_ratio:
      value_gt: 0.0686
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.06861 | 方向: data_verified (low=-0.013, high=-1.415)'
- id: gate_evt_var_99
  tag: HARD_EVT_VAR_99
  phase: hard_gate
  priority: 15
  reason: evt_var_99 触发树分裂条件 (分裂 2 次)
  when:
    evt_var_99:
      value_lt: 0.7599
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.7599 | 方向: data_verified (low=-1.052, high=0.478)'
- id: gate_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 16
  reason: evt_tail_shape 触发树分裂条件 (分裂 2 次)
  when:
    evt_tail_shape:
      value_gt: 0.3948
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.3948 | 方向: data_verified (low=-0.233, high=-0.820)'
- id: gate_evt_scale
  tag: HARD_EVT_SCALE
  phase: hard_gate
  priority: 17
  reason: evt_scale 触发树分裂条件 (分裂 2 次)
  when:
    evt_scale:
      value_gt: 0.9345
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.9345 | 方向: data_verified (low=-0.466, high=-1.381)'
- id: gate_evt_scale
  tag: HARD_EVT_SCALE
  phase: hard_gate
  priority: 18
  reason: evt_scale 触发树分裂条件 (分裂 2 次)
  when:
    evt_scale:
      value_lt: 0.4464
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 0.4464 | 方向: data_verified (low=-1.393, high=0.009)'
- id: gate_vpin_momentum
  tag: HARD_VPIN_MOMENTUM
  phase: hard_gate
  priority: 19
  reason: vpin_momentum 触发树分裂条件 (分裂 2 次)
  when:
    vpin_momentum:
      value_gt: -0.0206
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 -0.02057 | 方向: data_verified (low=-0.119, high=-0.779)'
guardrails: []

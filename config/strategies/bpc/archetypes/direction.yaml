# 生产方向 — VWAP1200 单锚带通：贴 VWAP 过近 / 过度延伸均不开仓；对侧极端留给 FER。
# 候选与评估见 ../features_direction.yaml

description: "BPC 方向 — macro_tp_vwap_1200_position 带通 (inner<|pos|<outer)"

direction_rules:
  - method: single_position_band
    id: vwap1200_band
    feature: macro_tp_vwap_1200_position
    inner_abs: 0.005
    outer_abs: 0.05
    description: "内侧死区与外侧过度延伸均方向=0；中间段多/空由符号决定"
    locked: true
    lock_reason: BPC 语义锚点 VWAP1200 带通

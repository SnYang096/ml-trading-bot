# BPC Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/bpc/20260304_201913_s3/strategies/bpc/gate_draft.yaml
# 优化规则: 2/10 条通过优化, 8 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_bpc_volume_compression_pct
  tag: PREFILTER_BPC_VOLUME_COMPRESSION_PCT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_volume_compression_pct <= 0.1503'
  when:
    bpc_volume_compression_pct:
      value_gt: 0.1503
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_bpc_bb_compression
  tag: PREFILTER_BPC_BB_COMPRESSION
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_bb_compression > 0.0965'
  when:
    bpc_bb_compression:
      value_le: 0.0965
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_bpc_cvd_z
  tag: PREFILTER_BPC_CVD_Z
  phase: hard_gate
  priority: 1
  reason: 'prefilter: bpc_cvd_z > -2.0018'
  when:
    bpc_cvd_z:
      value_le: -2.0018
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_evt_var_99_left
  tag: HARD_EVT_VAR_99_LEFT
  phase: hard_gate
  priority: 14
  reason: evt_var_99_left 触发树分裂条件 (分裂 2 次)
  when:
    evt_var_99_left:
      value_lt: 0.7159
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.7159, lift=0.176'
- id: gate_evt_var_99_left
  tag: HARD_EVT_VAR_99_LEFT
  phase: hard_gate
  priority: 18
  reason: evt_var_99_left 触发树分裂条件 (分裂 2 次)
  when:
    evt_var_99_left:
      value_lt: 0.7159
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.7159, lift=0.176'
guardrails: []

# BPC Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-06
# 数据源: features_labeled.parquet, 39126 行

rules:
  - feature: funding_ignition_score
    operator: ">"
    value: 0.0236
    rationale: "meta-algorithm, lift=1.20, robustness=96%, gate_score=0.053, holdout验证通过"
  - feature: bpc_bb_compression
    operator: "<"
    value: 0.1837
    rationale: "meta-algorithm, lift=1.12, robustness=92%, gate_score=0.042, holdout验证通过"
  - feature: bpc_vpin_breakout_confirm
    operator: "<"
    value: 0.3135
    rationale: "meta-algorithm, lift=1.16, robustness=90%, gate_score=0.014, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-06) ──
  timestamp: "2026-03-06"
  data_source: "features_labeled.parquet"
  n_rows: 39126
  baseline_bad_rate: 0.4515
  baseline_median_rr: -0.0756

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


# BPC Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-07
# 数据源: features_labeled.parquet, 39768 行

rules:
  - feature: bpc_garch_compression
    operator: ">"
    value: 0.98
    rationale: "meta-algorithm, KS=0.1978, return_uplift=-0.0930, robustness=100%, holdout验证通过"
  - feature: bpc_volume_compression_pct
    operator: "<"
    value: 0.0204
    rationale: "meta-algorithm, KS=0.1353, return_uplift=+0.0164, robustness=100%, holdout验证通过"
  - feature: bpc_vol_compression_state
    operator: ">="
    value: 0.17
    rationale: "range lower, meta-algorithm, KS=0.0988, return_uplift=+0.2554, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-07) ──
  timestamp: "2026-03-07"
  data_source: "features_labeled.parquet"
  n_rows: 39768
  baseline_bad_rate: 0.0000
  baseline_median_rr: -0.0475

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


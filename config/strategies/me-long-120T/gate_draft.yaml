# ME-LONG-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260324_212342_rr_extreme/me-long-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-long-120T
#   python scripts/optimize_gate_unified.py --strategy me-long-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_recent_compression_decay_recent_compression_decay
  tag: HARD_RECENT_COMPRESSION_DECAY_RECENT_COMPRESSION_DECAY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.31)
  when:
    all_of:
    - recent_compression_decay:
        value_gt: 0.5408
    - recent_compression_decay:
        value_lt: 0.7315
  then:
    action: deny
  comment: 'statistical(lift=1.31): recent_compression_decay > 0.5408 & recent_compression_decay < 0.7315'
- id: gate_shd_pct
  tag: HARD_SHD_PCT
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.06)
  when:
    shd_pct:
      value_gt: 0.679
  then:
    action: deny
  comment: 'statistical(lift=1.06): shd_pct > 0.679'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.0558
    - vol_leverage_asymmetry:
        value_lt: 0.1482
  then:
    action: deny
  comment: 'statistical(lift=1.08): vol_leverage_asymmetry > 0.05582 & vol_leverage_asymmetry < 0.1482'
- id: gate_evt_es_99_evt_es_99
  tag: HARD_EVT_ES_99_EVT_ES_99
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.12)
  when:
    all_of:
    - evt_es_99:
        value_gt: 0.3532
    - evt_es_99:
        value_lt: 0.5119
  then:
    action: deny
  comment: 'statistical(lift=1.12): evt_es_99 > 0.3532 & evt_es_99 < 0.5119'
- id: gate_vpin_max20_vpin_max20
  tag: HARD_VPIN_MAX20_VPIN_MAX20
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.19)
  when:
    all_of:
    - vpin_max20:
        value_gt: 0.4486
    - vpin_max20:
        value_lt: 0.4799
  then:
    action: deny
  comment: 'statistical(lift=1.19): vpin_max20 > 0.4486 & vpin_max20 < 0.4799'
guardrails: []

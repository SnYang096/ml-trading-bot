# FER-LONG Gate Draft (auto-generated)
# 来源: results/train_final_20260314_213454_rr_extreme/fer-long
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-long
#   python scripts/optimize_gate_unified.py --strategy fer-long --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_atr_percentile_atr_percentile
  tag: HARD_ATR_PERCENTILE_ATR_PERCENTILE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.53)
  when:
    all_of:
    - atr_percentile:
        value_gt: 0.0278
    - atr_percentile:
        value_lt: 0.3563
  then:
    action: deny
  comment: 'statistical(lift=1.53): atr_percentile > 0.02778 & atr_percentile < 0.3563'
- id: gate_macd_signal_atr
  tag: HARD_MACD_SIGNAL_ATR
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.77)
  when:
    macd_signal_atr:
      value_gt: 0.3125
  then:
    action: deny
  comment: 'statistical(lift=1.77): macd_signal_atr > 0.3125'
- id: gate_vp_ignition_score
  tag: HARD_VP_IGNITION_SCORE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.11)
  when:
    vp_ignition_score:
      value_lt: 0.0024
  then:
    action: deny
  comment: 'statistical(lift=1.11): vp_ignition_score < 0.002441'
- id: gate_vpin_ignition_score_vpin_ignition_score
  tag: HARD_VPIN_IGNITION_SCORE_VPIN_IGNITION_SCORE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.36)
  when:
    all_of:
    - vpin_ignition_score:
        value_gt: 0.0366
    - vpin_ignition_score:
        value_lt: 0.0524
  then:
    action: deny
  comment: 'statistical(lift=1.36): vpin_ignition_score > 0.0366 & vpin_ignition_score < 0.05243'
guardrails: []

# LV Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-13
# 数据源: features_labeled.parquet, 445875 行

rules:
  - feature: funding_rate_zscore_50
    operator: "<="
    value: -1.727
    rationale: "meta-algorithm, KS=0.1968, return_uplift=+1.5192, robustness=100%, holdout验证通过"
  - feature: dist_to_nearest_sr
    operator: "<="
    value: 5.142
    rationale: "meta-algorithm, KS=0.1076, return_uplift=+1.5210, robustness=100%, holdout验证通过"
  - feature: funding_rate_abs_zscore_50
    operator: ">="
    value: 1.085
    rationale: "range lower, meta-algorithm, KS=0.1061, return_uplift=+1.2713, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-13) ──
  timestamp: "2026-03-13"
  data_source: "features_labeled.parquet"
  n_rows: 445875
  baseline_bad_rate: 0.0000
  baseline_median_rr: -0.0112

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


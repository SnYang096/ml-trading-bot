# ME Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/me-long/20260313_131754/strategies/me-long/gate_draft.yaml
# 优化规则: 0/4 条通过优化, 2 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vpin_max20_vpin_max20
  tag: HARD_VPIN_MAX20_VPIN_MAX20
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.17)
  when:
    all_of:
    - vpin_max20:
        value_gt: 0.2952
    - vpin_max20:
        value_lt: 0.3332
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.3250, lift=0.678'
- id: gate_evt_scale_left_evt_scale_left
  tag: HARD_EVT_SCALE_LEFT_EVT_SCALE_LEFT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.13)
  when:
    all_of:
    - evt_scale_left:
        value_gt: 0.6667
    - evt_scale_left:
        value_lt: 0.7897
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4738, lift=0.430'
guardrails: []

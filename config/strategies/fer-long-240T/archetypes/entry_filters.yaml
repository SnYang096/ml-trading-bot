# FER Entry Filter Archetype
# Auto-promoted by optimize_entry_filter_plateau.py --meta-algorithm
# Date: 2026-03-06 00:13
# Promoted: 2 filters (2 plateau + 0 snotio-only)
# Baseline snotio: 3.8289, σ=9.533
# Significance: z-test p<0.05 required for all tiers
#
# 职责: "现在该入场吗?" → 硬二值控制入场时机
# 多个 filter 之间是 OR 关系
# Tier A = plateau + 显著, Tier B = snotio + 显著 (no plateau)

filters:
- id: ef_shd_pct_0.2
  enabled: true
  description: 'auto: shd_pct <= 0.076809'
  conditions:
  - feature: shd_pct
    operator: <=
    value: 0.076809
  notes: method=distribution_ks, effect=+5.6829, ks=0.424, rob=0.67, pass=20.2%
- id: ef_funding_oi_crowding_score_0.75
  enabled: true
  description: 'auto: funding_oi_crowding_score >= 0.2573'
  conditions:
  - feature: funding_oi_crowding_score
    operator: '>='
    value: 0.2573
  notes: method=distribution_ks, effect=+7.3946, ks=0.403, rob=0.67, pass=25.1%
- id: ef_funding_absorption_score_0.7
  enabled: true
  description: 'auto: funding_absorption_score >= 0.178185'
  conditions:
  - feature: funding_absorption_score
    operator: '>='
    value: 0.178185
  notes: method=distribution_ks, effect=+2.9876, ks=0.369, rob=0.67, pass=30.1%
- id: ef_trade_cluster_net_runs_ma5_0.8
  enabled: true
  description: 'auto: trade_cluster_net_runs_ma5 >= 0.004'
  conditions:
  - feature: trade_cluster_net_runs_ma5
    operator: '>='
    value: 0.004
  notes: method=distribution_ks, effect=+6.0782, ks=0.359, rob=1.00, pass=20.2%
- id: ef_fp_delta_skew_0.8
  enabled: true
  description: 'auto: fp_delta_skew >= 0.393916'
  conditions:
  - feature: fp_delta_skew
    operator: '>='
    value: 0.393916
  notes: method=distribution_ks, effect=+4.3870, ks=0.333, rob=0.67, pass=20.2%
combination_mode: or

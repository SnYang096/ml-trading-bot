# ME-LONG-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260325_131042_rr_extreme/me-long-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-long-240T
#   python scripts/optimize_gate_unified.py --strategy me-long-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vp_width_ratio
  tag: HARD_VP_WIDTH_RATIO
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.58)
  when:
    vp_width_ratio:
      value_lt: 0.4713
  then:
    action: deny
  comment: 'statistical(lift=1.58): vp_width_ratio < 0.4713'
- id: gate_fer_signed_efficiency_pct
  tag: HARD_FER_SIGNED_EFFICIENCY_PCT
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.22)
  when:
    fer_signed_efficiency_pct:
      value_gt: 0.75
  then:
    action: deny
  comment: 'statistical(lift=1.22): fer_signed_efficiency_pct > 0.75'
- id: gate_vol_persistence
  tag: HARD_VOL_PERSISTENCE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.49)
  when:
    vol_persistence:
      value_lt: 0.0004
  then:
    action: deny
  comment: 'statistical(lift=1.49): vol_persistence < 0.0004249'
- id: gate_me_vol_regime
  tag: HARD_ME_VOL_REGIME
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.28)
  when:
    me_vol_regime:
      value_gt: 0.77
  then:
    action: deny
  comment: 'statistical(lift=1.28): me_vol_regime > 0.77'
- id: gate_evt_scale_left
  tag: HARD_EVT_SCALE_LEFT
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.33)
  when:
    evt_scale_left:
      value_lt: 0.0952
  then:
    action: deny
  comment: 'statistical(lift=1.33): evt_scale_left < 0.09524'
guardrails: []

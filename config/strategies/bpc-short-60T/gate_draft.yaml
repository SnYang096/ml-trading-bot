# BPC-SHORT-60T Gate Draft (auto-generated)
# 来源: results/train_final_20260317_210914_rr_extreme/bpc-short-60T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-short-60T
#   python scripts/optimize_gate_unified.py --strategy bpc-short-60T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_scale_left
  tag: HARD_EVT_SCALE_LEFT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.32)
  when:
    evt_scale_left:
      value_lt: 0.1071
  then:
    action: deny
  comment: 'statistical(lift=1.32): evt_scale_left < 0.1071'
- id: gate_vpin_ma20
  tag: HARD_VPIN_MA20
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.15)
  when:
    vpin_ma20:
      value_gt: 0.3224
  then:
    action: deny
  comment: 'statistical(lift=1.15): vpin_ma20 > 0.3224'
- id: gate_evt_es_99_left
  tag: HARD_EVT_ES_99_LEFT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.09)
  when:
    evt_es_99_left:
      value_gt: 0.8056
  then:
    action: deny
  comment: 'statistical(lift=1.09): evt_es_99_left > 0.8056'
- id: gate_fer_absorption_streak
  tag: HARD_FER_ABSORPTION_STREAK
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.07)
  when:
    fer_absorption_streak:
      value_gt: 0.815
  then:
    action: deny
  comment: 'statistical(lift=1.07): fer_absorption_streak > 0.815'
- id: gate_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.11)
  when:
    fer_signed_efficiency:
      value_lt: -0.0
  then:
    action: deny
  comment: 'statistical(lift=1.11): fer_signed_efficiency < -1.202e-05'
guardrails: []

# FER-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260323_145232_rr_extreme/fer-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-120T
#   python scripts/optimize_gate_unified.py --strategy fer-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_atr_percentile
  tag: HARD_ATR_PERCENTILE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.42)
  when:
    atr_percentile:
      value_lt: 0.1759
  then:
    action: deny
  comment: 'statistical(lift=1.42): atr_percentile < 0.1759'
- id: gate_evt_es_99
  tag: HARD_EVT_ES_99
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.20)
  when:
    evt_es_99:
      value_gt: 0.7976
  then:
    action: deny
  comment: 'statistical(lift=1.20): evt_es_99 > 0.7976'
- id: gate_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.28)
  when:
    evt_tail_shape:
      value_lt: 0.0397
  then:
    action: deny
  comment: 'statistical(lift=1.28): evt_tail_shape < 0.03968'
- id: gate_evt_scale_right_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.12)
  when:
    all_of:
    - evt_scale_right:
        value_gt: 0.0913
    - evt_scale_right:
        value_lt: 0.2778
  then:
    action: deny
  comment: 'statistical(lift=1.12): evt_scale_right > 0.09127 & evt_scale_right < 0.2778'
- id: gate_fer_signed_efficiency_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.12)
  when:
    all_of:
    - fer_signed_efficiency:
        value_gt: 0.0
    - fer_signed_efficiency:
        value_lt: 0.0
  then:
    action: deny
  comment: 'statistical(lift=1.12): fer_signed_efficiency > 0 & fer_signed_efficiency < 3.255e-06'
guardrails: []

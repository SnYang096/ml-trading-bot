# FER-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260318_021415_rr_extreme/fer-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-120T
#   python scripts/optimize_gate_unified.py --strategy fer-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.31)
  when:
    evt_tail_shape:
      value_lt: 0.0754
  then:
    action: deny
  comment: 'statistical(lift=1.31): evt_tail_shape < 0.0754'
- id: gate_evt_scale_evt_scale
  tag: HARD_EVT_SCALE_EVT_SCALE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.25)
  when:
    all_of:
    - evt_scale:
        value_gt: 0.3492
    - evt_scale:
        value_lt: 0.5238
  then:
    action: deny
  comment: 'statistical(lift=1.25): evt_scale > 0.3492 & evt_scale < 0.5238'
- id: gate_evt_tail_shape_right
  tag: HARD_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.10)
  when:
    evt_tail_shape_right:
      value_lt: 0.1706
  then:
    action: deny
  comment: 'statistical(lift=1.10): evt_tail_shape_right < 0.1706'
- id: gate_vol_persistence_vol_persistence
  tag: HARD_VOL_PERSISTENCE_VOL_PERSISTENCE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.26)
  when:
    all_of:
    - vol_persistence:
        value_gt: 0.0216
    - vol_persistence:
        value_lt: 0.0539
  then:
    action: deny
  comment: 'statistical(lift=1.26): vol_persistence > 0.02163 & vol_persistence < 0.05394'
- id: gate_evt_var_99_evt_var_99
  tag: HARD_EVT_VAR_99_EVT_VAR_99
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.24)
  when:
    all_of:
    - evt_var_99:
        value_gt: 0.7857
    - evt_var_99:
        value_lt: 0.8968
  then:
    action: deny
  comment: 'statistical(lift=1.24): evt_var_99 > 0.7857 & evt_var_99 < 0.8968'
guardrails: []

# FER-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260317_091636_rr_extreme/fer-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer-short-120T
#   python scripts/optimize_gate_unified.py --strategy fer-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vol_persistence
  tag: HARD_VOL_PERSISTENCE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.61)
  when:
    vol_persistence:
      value_gt: 0.1794
  then:
    action: deny
  comment: 'statistical(lift=1.61): vol_persistence > 0.1794'
- id: gate_evt_tail_shape_right
  tag: HARD_EVT_TAIL_SHAPE_RIGHT
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.14)
  when:
    evt_tail_shape_right:
      value_gt: 0.4087
  then:
    action: deny
  comment: 'statistical(lift=1.14): evt_tail_shape_right > 0.4087'
- id: gate_evt_es_99_evt_es_99
  tag: HARD_EVT_ES_99_EVT_ES_99
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.60)
  when:
    all_of:
    - evt_es_99:
        value_gt: 0.1865
    - evt_es_99:
        value_lt: 0.2659
  then:
    action: deny
  comment: 'statistical(lift=1.60): evt_es_99 > 0.1865 & evt_es_99 < 0.2659'
- id: gate_evt_tail_shape_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.23)
  when:
    all_of:
    - evt_tail_shape:
        value_gt: 0.1389
    - evt_tail_shape:
        value_lt: 0.4603
  then:
    action: deny
  comment: 'statistical(lift=1.23): evt_tail_shape > 0.1389 & evt_tail_shape < 0.4603'
- id: gate_vpin_volatility_20_vpin_volatility_20
  tag: HARD_VPIN_VOLATILITY_20_VPIN_VOLATILITY_20
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.39)
  when:
    all_of:
    - vpin_volatility_20:
        value_gt: 0.0451
    - vpin_volatility_20:
        value_lt: 0.0505
  then:
    action: deny
  comment: 'statistical(lift=1.39): vpin_volatility_20 > 0.04509 & vpin_volatility_20 < 0.05054'
guardrails: []

# Chop grid research artifacts

Threshold / search sketches live next to the strategy root for now:

- `../threshold_search.yaml`
- `../research.yaml`

Rolling exports adoptable bundles under `results/chop_grid/<history>/chop_grid/<timestamp>/strategies/chop_grid/` (see `auto_research_pipeline` multileg export).

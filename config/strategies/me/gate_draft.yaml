# ME Gate Draft (auto-generated)
# 来源: results/train_final_20260312_000352_rr_extreme/me
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me
#   python scripts/optimize_gate_unified.py --strategy me --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_var_99_right
  tag: HARD_EVT_VAR_99_RIGHT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.17)
  when:
    evt_var_99_right:
      value_lt: 0.0595
  then:
    action: deny
  comment: 'statistical(lift=1.17): evt_var_99_right < 0.05952'
- id: gate_recent_compression_decay
  tag: HARD_RECENT_COMPRESSION_DECAY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.18)
  when:
    recent_compression_decay:
      value_gt: 0.7232
  then:
    action: deny
  comment: 'statistical(lift=1.18): recent_compression_decay > 0.7232'
guardrails: []

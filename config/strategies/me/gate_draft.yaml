# ME Gate Draft (auto-generated)
# 来源: results/train_final_20260308_203537_rr_extreme/me
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me
#   python scripts/optimize_gate_unified.py --strategy me --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_atr_percentile_atr_percentile
  tag: HARD_ATR_PERCENTILE_ATR_PERCENTILE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.10)
  when:
    all_of:
    - atr_percentile:
        value_gt: 0.213
    - atr_percentile:
        value_lt: 0.3
  then:
    action: deny
  comment: 'statistical(lift=1.10): atr_percentile > 0.213 & atr_percentile < 0.3'
- id: gate_garch_volatility_garch_volatility
  tag: HARD_GARCH_VOLATILITY_GARCH_VOLATILITY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - garch_volatility:
        value_gt: 0.0038
    - garch_volatility:
        value_lt: 0.0046
  then:
    action: deny
  comment: 'statistical(lift=1.08): garch_volatility > 0.003794 & garch_volatility < 0.004564'
guardrails: []

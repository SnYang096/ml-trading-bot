# ME-Long (MomentumExpansion Long) 策略元信息

strategy:
  name: me-long-120T
  description: |
    MomentumExpansion long archetype: 流动性加速 + 波动扩张驱动（做多方向）。

  owner: quant_team
  archetype: MomentumExpansionLong

  # 默认时间粒度（研发 + 实盘统一）
  timeframe: "120T"

  # 见 bpc/meta.yaml：macro_tp_vwap_1200_position 默认锚定 BTCUSDT。
  macro_tp_vwap_anchor:
    enabled: false
    symbol: BTCUSDT

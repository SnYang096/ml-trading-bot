# ME Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/me/20260304_121535_s4/strategies/me/gate_draft.yaml
# 优化规则: 5/10 条通过优化, 9 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_atr_percentile
  tag: PREFILTER_ATR_PERCENTILE
  phase: hard_gate
  priority: 1
  reason: 'prefilter: atr_percentile >= 0.8167'
  when:
    atr_percentile:
      value_lt: 0.8167
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_atr_pct
  tag: PREFILTER_ME_ATR_PCT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_atr_pct > 0.01'
  when:
    me_atr_pct:
      value_le: 0.01
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_cvd_alignment
  tag: PREFILTER_ME_CVD_ALIGNMENT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_cvd_alignment >= 1.0'
  when:
    me_cvd_alignment:
      value_lt: 1.0
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_hilbert_cvd_price_env_ratio
  tag: HARD_HILBERT_CVD_PRICE_ENV_RATIO
  phase: hard_gate
  priority: 14
  reason: hilbert_cvd_price_env_ratio 触发树分裂条件 (分裂 3 次)
  when:
    hilbert_cvd_price_env_ratio:
      value_gt: -0.2876
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=-0.2876, lift=0.189'
guardrails: []

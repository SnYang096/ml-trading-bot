# ME Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/me/20260226_211920_s42/strategies/me/gate_draft.yaml
# 优化规则: 5/10 条通过优化, 7 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_atr_percentile
  tag: PREFILTER_ATR_PERCENTILE
  phase: hard_gate
  priority: 1
  reason: 'prefilter: atr_percentile >= 0.8167'
  when:
    atr_percentile:
      value_lt: 0.8167
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_atr_pct
  tag: PREFILTER_ME_ATR_PCT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_atr_pct > 0.01'
  when:
    me_atr_pct:
      value_le: 0.01
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_cvd_alignment
  tag: PREFILTER_ME_CVD_ALIGNMENT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_cvd_alignment >= 1.0'
  when:
    me_cvd_alignment:
      value_lt: 1.0
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_evt_scale
  tag: HARD_EVT_SCALE
  phase: hard_gate
  priority: 19
  reason: evt_scale 触发树分裂条件 (分裂 3 次)
  when:
    evt_scale:
      value_lt: 0.601
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6010, lift=0.184'
- id: gate_evt_var_99
  tag: HARD_EVT_VAR_99
  phase: hard_gate
  priority: 16
  reason: evt_var_99 触发树分裂条件 (分裂 3 次)
  when:
    evt_var_99:
      value_lt: 0.6829
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6829, lift=0.234'
- id: gate_vp_width_ratio
  tag: HARD_VP_WIDTH_RATIO
  phase: hard_gate
  priority: 15
  reason: vp_width_ratio 触发树分裂条件 (分裂 3 次)
  when:
    vp_width_ratio:
      value_lt: 0.5814
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=0.5814, lift=0.415'
guardrails: []

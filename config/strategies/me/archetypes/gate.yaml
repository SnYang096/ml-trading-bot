# ME Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/me/20260301_223817_s42/strategies/me/gate_draft.yaml
# 优化规则: 2/10 条通过优化, 8 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_atr_percentile
  tag: PREFILTER_ATR_PERCENTILE
  phase: hard_gate
  priority: 1
  reason: 'prefilter: atr_percentile >= 0.8167'
  when:
    atr_percentile:
      value_lt: 0.8167
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_atr_pct
  tag: PREFILTER_ME_ATR_PCT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_atr_pct > 0.01'
  when:
    me_atr_pct:
      value_le: 0.01
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: prefilter_me_cvd_alignment
  tag: PREFILTER_ME_CVD_ALIGNMENT
  phase: hard_gate
  priority: 1
  reason: 'prefilter: me_cvd_alignment >= 1.0'
  when:
    me_cvd_alignment:
      value_lt: 1.0
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_evt_tail_shape_left
  tag: HARD_EVT_TAIL_SHAPE_LEFT
  phase: hard_gate
  priority: 12
  reason: evt_tail_shape_left 触发树分裂条件 (分裂 1 次)
  when:
    evt_tail_shape_left:
      value_lt: 0.704
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.7040, lift=0.153'
- id: gate_evt_es_99_right
  tag: HARD_EVT_ES_99_RIGHT
  phase: hard_gate
  priority: 16
  reason: evt_es_99_right 触发树分裂条件 (分裂 1 次)
  when:
    evt_es_99_right:
      value_lt: 0.6665
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6665, lift=0.184'
guardrails: []

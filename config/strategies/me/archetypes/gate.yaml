# ME Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/me/20260224_125251/strategies/me/gate_draft.yaml
# 优化规则: 2/10 条通过优化, 8 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: gate_shd_pct
  tag: HARD_SHD_PCT
  phase: hard_gate
  priority: 10
  reason: shd_pct 触发树分裂条件 (分裂 2 次)
  when:
    shd_pct:
      value_gt: 0.55
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.5500, lift=0.244'
- id: gate_evt_scale
  tag: HARD_EVT_SCALE
  phase: hard_gate
  priority: 18
  reason: evt_scale 触发树分裂条件 (分裂 1 次)
  when:
    evt_scale:
      value_lt: 0.6004
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6004, lift=0.379'
guardrails: []

# ME Gate — 动量扩张专用
#
# 2026-04 全面修正：移除 5 条「与 BPC 对齐」的 locked 规则。
# BPC 做回调、ME 做趋势延续，语义相反；BPC 的 EVT/VPIN/vol_persistence band
# 在动量主升段拦截 48-54%（SOL/XRP 2024-11 实测）。
#
# 清空 locked hard_gates，让 rolling 标定（optimize_gate_unified）从当月数据
# 自行发现适合 ME 的 gate 规则（如 VWAP position、vol_clustering 等）。
schema:
  phases:
    - system_safety
    - hard_gate
    - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates: []
guardrails: []

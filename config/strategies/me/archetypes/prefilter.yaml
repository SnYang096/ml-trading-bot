# ME Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-12
# 数据源: features_labeled.parquet, 111618 行

# ⚠️  无法自动生成 rules, 请手动配置
# rules:
#   - feature: xxx
#     operator: ">="
#     value: 0.0

last_evaluation:
  # ── 自动生成 (2026-03-12) ──
  timestamp: "2026-03-12"
  data_source: "features_labeled.parquet"
  n_rows: 111618
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.0217

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


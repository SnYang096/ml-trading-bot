# ME Prefilter（双边动量扩张）— archetype 环境门槛，多空由 direction 决定
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 源自自动生成底稿 (2026-03-13)；加速度规则已改为多空任一满足。

rules:
  - feature: me_atr_pct
    operator: ">="
    value: 0.65
    locked: true
    lock_reason: "ME语义：仅在中高波扩张区参与，避免低波噪声"

  - feature: me_atr_pct
    operator: "<="
    value: 0.99
    locked: true
    lock_reason: "ME语义：避免极端尾段过热后追单"

  - feature: me_cvd_alignment
    operator: ">="
    value: 0.55
    locked: true
    lock_reason: "ME语义：价流短期一致（涨时同向吸货/跌时同向抛压），双边均要求参与确认"

last_evaluation:
  # ── 自动生成 (2026-03-13) ──
  timestamp: "2026-03-13"
  data_source: "features_labeled.parquet"
  n_rows: 112361
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.0276

  # ── 正信号 (0 个) ──
  positive_signals: []

  # ── 反信号 (0 个) ──
  anti_signals: []

  # ── 低端信号 (0 个) ──
  absence_signals: []

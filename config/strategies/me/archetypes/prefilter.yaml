# ME Archetype Prefilter (auto-generated, Bell Partition)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-11
# 数据源: features_labeled.parquet, 156974 行
# 组合结构: Bell Partition (组内 OR, 组间 AND)

rules: []

last_evaluation:
  # ── 自动生成 (2026-03-11) ──
  timestamp: "2026-03-11"
  data_source: "features_labeled.parquet"
  n_rows: 156974
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.0613

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


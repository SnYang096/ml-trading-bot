# ME Entry Filter Archetype
# Auto-promoted by optimize_entry_filter_plateau.py (统计方法)
# Date: 2026-05-12 08:28
# Promoted: 1 filters (method=upside_positive_rate_ratio)
# Locked pool: 0 filters (missing ones auto-disabled)
# Significance: 统计验证 + robustness ≥ 0.5
#
# 职责: "现在该入场吗?" → 硬二值控制
# 多个 filter 之间是 OR 关系

filters:
- id: ef_cvd_divergence_score_pct_0.8
  enabled: true
  description: 'auto: cvd_divergence_score_pct >= 0.821892'
  conditions:
  - feature: cvd_divergence_score_pct
    operator: '>='
    value: 0.821892
  notes: method=upside_positive_rate_ratio, effect=+1.8440, rob=1.00, pass=20.2%
combination_mode: or

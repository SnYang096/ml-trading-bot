# ME Entry Filter Archetype
# Auto-promoted by optimize_entry_filter_plateau.py --meta-algorithm
# Date: 2026-03-06 01:41
# Promoted: 1 filters (1 plateau + 0 snotio-only)
# Baseline snotio: 8.6927, σ=14.307
# Significance: z-test p<0.05 required for all tiers
# ⚠️ 2026-03-10 手动恢复: 3/8 pipeline 用 evidence bins OR 替换导致 pass rate ~95%
#
# 职责: "现在该入场吗?" → 硬二值控制入场时机
# 多个 filter 之间是 OR 关系
# Tier A = plateau + 显著, Tier B = snotio + 显著 (no plateau)

filters:
- id: meta_funding_rate_zscore_50
  enabled: true
  description: 'Meta-Algorithm: funding_rate_zscore_50 >= 0.873'
  conditions:
  - feature: funding_rate_zscore_50
    operator: '>='
    value: 0.873
  tier: A_PLATEAU
  notes: lift=1.07x, rob=75%, snotio=14.5397, z=3.72, p=0.0001
combination_mode: or

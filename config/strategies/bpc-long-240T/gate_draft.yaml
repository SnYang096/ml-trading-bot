# BPC-LONG-240T Gate Draft (auto-generated)
# 来源: results/train_final_20260325_130032_rr_extreme/bpc-long-240T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-long-240T
#   python scripts/optimize_gate_unified.py --strategy bpc-long-240T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vp_entropy
  tag: HARD_VP_ENTROPY
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.40)
  when:
    vp_entropy:
      value_gt: 0.9612
  then:
    action: deny
  comment: 'statistical(lift=1.40): vp_entropy > 0.9612'
- id: gate_vp_hv_ratio
  tag: HARD_VP_HV_RATIO
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.07)
  when:
    vp_hv_ratio:
      value_lt: 0.25
  then:
    action: deny
  comment: 'statistical(lift=1.07): vp_hv_ratio < 0.25'
- id: gate_fer_signed_efficiency_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.10)
  when:
    all_of:
    - fer_signed_efficiency:
        value_gt: -0.0
    - fer_signed_efficiency:
        value_lt: 0.0
  then:
    action: deny
  comment: 'statistical(lift=1.10): fer_signed_efficiency > -1.59e-05 & fer_signed_efficiency < 1.152e-09'
- id: gate_fer_efficiency_flip_fer_efficiency_flip
  tag: HARD_FER_EFFICIENCY_FLIP_FER_EFFICIENCY_FLIP
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.12)
  when:
    all_of:
    - fer_efficiency_flip:
        value_gt: 0.0339
    - fer_efficiency_flip:
        value_lt: 0.0651
  then:
    action: deny
  comment: 'statistical(lift=1.12): fer_efficiency_flip > 0.03389 & fer_efficiency_flip < 0.06511'
- id: gate_fer_trapped_shorts_score_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.09)
  when:
    all_of:
    - fer_trapped_shorts_score:
        value_gt: 0.2725
    - fer_trapped_shorts_score:
        value_lt: 0.4215
  then:
    action: deny
  comment: 'statistical(lift=1.09): fer_trapped_shorts_score > 0.2725 & fer_trapped_shorts_score < 0.4215'
guardrails: []

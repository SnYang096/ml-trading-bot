# BPC-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260331_225118_rr_extreme/bpc-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-short-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_oi_compression_score_oi_compression_score
  tag: HARD_OI_COMPRESSION_SCORE_OI_COMPRESSION_SCORE
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.38)
  when:
    all_of:
    - oi_compression_score:
        value_gt: 0.0771
    - oi_compression_score:
        value_lt: 0.1602
  then:
    action: deny
  comment: 'statistical(lift=1.38): oi_compression_score > 0.07707 & oi_compression_score < 0.1602'
- id: gate_spectrum_volume_flatness_spectrum_volume_flatness
  tag: HARD_SPECTRUM_VOLUME_FLATNESS_SPECTRUM_VOLUME_FLATNESS
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.32)
  when:
    all_of:
    - spectrum_volume_flatness:
        value_gt: 0.5691
    - spectrum_volume_flatness:
        value_lt: 0.63
  then:
    action: deny
  comment: 'statistical(lift=1.32): spectrum_volume_flatness > 0.5691 & spectrum_volume_flatness < 0.63'
- id: gate_funding_rate_abs_funding_rate_abs
  tag: HARD_FUNDING_RATE_ABS_FUNDING_RATE_ABS
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.43)
  when:
    all_of:
    - funding_rate_abs:
        value_gt: 0.0
    - funding_rate_abs:
        value_lt: 0.0001
  then:
    action: deny
  comment: 'statistical(lift=1.43): funding_rate_abs > 4.794e-05 & funding_rate_abs < 9.474e-05'
- id: gate_oi_ignition_score
  tag: HARD_OI_IGNITION_SCORE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.40)
  when:
    oi_ignition_score:
      value_gt: 0.0474
  then:
    action: deny
  comment: 'statistical(lift=1.40): oi_ignition_score > 0.04744'
- id: gate_me_regime_suitable
  tag: HARD_ME_REGIME_SUITABLE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.26)
  when:
    me_regime_suitable:
      value_gt: 0.2669
  then:
    action: deny
  comment: 'statistical(lift=1.26): me_regime_suitable > 0.2669'
guardrails: []

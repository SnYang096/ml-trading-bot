# BPC-SHORT-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260326_125158_rr_extreme/bpc-short-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-short-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-short-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vp_width_ratio
  tag: HARD_VP_WIDTH_RATIO
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.31)
  when:
    vp_width_ratio:
      value_lt: 0.4822
  then:
    action: deny
  comment: 'statistical(lift=1.31): vp_width_ratio < 0.4822'
- id: gate_evt_tail_shape_left_evt_tail_shape_left
  tag: HARD_EVT_TAIL_SHAPE_LEFT_EVT_TAIL_SHAPE_LEFT
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.51)
  when:
    all_of:
    - evt_tail_shape_left:
        value_gt: 0.4126
    - evt_tail_shape_left:
        value_lt: 0.6151
  then:
    action: deny
  comment: 'statistical(lift=1.51): evt_tail_shape_left > 0.4126 & evt_tail_shape_left < 0.6151'
- id: gate_evt_var_99_right_evt_var_99_right
  tag: HARD_EVT_VAR_99_RIGHT_EVT_VAR_99_RIGHT
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.71)
  when:
    all_of:
    - evt_var_99_right:
        value_gt: 0.7431
    - evt_var_99_right:
        value_lt: 0.9563
  then:
    action: deny
  comment: 'statistical(lift=1.71): evt_var_99_right > 0.7431 & evt_var_99_right < 0.9563'
- id: gate_oi_exhaustion_score
  tag: HARD_OI_EXHAUSTION_SCORE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.25)
  when:
    oi_exhaustion_score:
      value_lt: 0.0036
  then:
    action: deny
  comment: 'statistical(lift=1.25): oi_exhaustion_score < 0.003588'
- id: gate_bpc_pullback_depth_long
  tag: HARD_BPC_PULLBACK_DEPTH_LONG
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.34)
  when:
    bpc_pullback_depth_long:
      value_lt: 0.037
  then:
    action: deny
  comment: 'statistical(lift=1.34): bpc_pullback_depth_long < 0.03696'
guardrails: []

# FER Gate Draft (auto-generated)
# 来源: results/train_final_20260304_210059_rr_extreme/fer
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer
#   python scripts/optimize_gate_unified.py --strategy fer --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: gate_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 10
  reason: fer_signed_efficiency 触发树分裂条件 (分裂 2 次)
  when:
    fer_signed_efficiency:
      value_lt: -0.0
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 2 次 | 阈值 -0 | 方向: data_verified (low=0.139, high=0.622)'
- id: gate_roc_5
  tag: HARD_ROC_5
  phase: hard_gate
  priority: 11
  reason: roc_5 触发树分裂条件 (分裂 1 次)
  when:
    roc_5:
      value_gt: -1.2039
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 -1.204 | 方向: data_verified (low=0.762, high=0.328)'
- id: gate_sma_200_position
  tag: HARD_SMA_200_POSITION
  phase: hard_gate
  priority: 12
  reason: sma_200_position 触发树分裂条件 (分裂 1 次)
  when:
    sma_200_position:
      value_gt: 0.0127
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.01272 | 方向: data_verified (low=0.468, high=0.241)'
- id: gate_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 13
  reason: fer_trapped_shorts_score 触发树分裂条件 (分裂 1 次)
  when:
    fer_trapped_shorts_score:
      value_gt: 0.4644
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.4644 | 方向: data_verified (low=1.052, high=0.040)'
- id: gate_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 14
  reason: evt_scale_right 触发树分裂条件 (分裂 1 次)
  when:
    evt_scale_right:
      value_lt: 0.4306
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.4306 | 方向: data_verified (low=-0.127, high=0.773)'
- id: gate_sma_200_position
  tag: HARD_SMA_200_POSITION
  phase: hard_gate
  priority: 15
  reason: sma_200_position 触发树分裂条件 (分裂 1 次)
  when:
    sma_200_position:
      value_gt: -0.0312
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 -0.03124 | 方向: data_verified (low=0.874, high=-0.116)'
- id: gate_roc_5
  tag: HARD_ROC_5
  phase: hard_gate
  priority: 16
  reason: roc_5 触发树分裂条件 (分裂 1 次)
  when:
    roc_5:
      value_gt: -1.3366
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 -1.337 | 方向: data_verified (low=0.708, high=0.348)'
- id: gate_evt_scale
  tag: HARD_EVT_SCALE
  phase: hard_gate
  priority: 17
  reason: evt_scale 触发树分裂条件 (分裂 1 次)
  when:
    evt_scale:
      value_gt: 0.2917
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.2917 | 方向: data_verified (low=1.467, high=0.012)'
- id: gate_fer_impulse_failure_score
  tag: HARD_FER_IMPULSE_FAILURE_SCORE
  phase: hard_gate
  priority: 18
  reason: fer_impulse_failure_score 触发树分裂条件 (分裂 1 次)
  when:
    fer_impulse_failure_score:
      value_gt: 0.9281
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.9281 | 方向: data_verified (low=0.424, high=0.083)'
- id: gate_fer_signed_efficiency_pct
  tag: HARD_FER_SIGNED_EFFICIENCY_PCT
  phase: hard_gate
  priority: 19
  reason: fer_signed_efficiency_pct 触发树分裂条件 (分裂 1 次)
  when:
    fer_signed_efficiency_pct:
      value_lt: 0.2466
  then:
    action: deny
  comment: '自动生成(tree_split fallback): 分裂 1 次 | 阈值 0.2466 | 方向: data_verified (low=-0.324, high=0.647)'
guardrails: []

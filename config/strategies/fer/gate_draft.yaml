# FER Gate Draft (auto-generated)
# 来源: results/train_final_20260308_205007_rr_extreme/fer
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy fer
#   python scripts/optimize_gate_unified.py --strategy fer --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_cvd_change_5_normalized
  tag: HARD_CVD_CHANGE_5_NORMALIZED
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.55)
  when:
    cvd_change_5_normalized:
      value_lt: -0.0455
  then:
    action: deny
  comment: 'statistical(lift=1.55): cvd_change_5_normalized < -0.04546'
- id: gate_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.10)
  when:
    fer_signed_efficiency:
      value_gt: 0.0012
  then:
    action: deny
  comment: 'statistical(lift=1.10): fer_signed_efficiency > 0.001208'
- id: gate_fer_volume_price_divergence_fer_volume_price_divergence
  tag: HARD_FER_VOLUME_PRICE_DIVERGENCE_FER_VOLUME_PRICE_DIVERGENCE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.06)
  when:
    all_of:
    - fer_volume_price_divergence:
        value_gt: 0.425
    - fer_volume_price_divergence:
        value_lt: 0.675
  then:
    action: deny
  comment: 'statistical(lift=1.06): fer_volume_price_divergence > 0.425 & fer_volume_price_divergence < 0.675'
guardrails: []

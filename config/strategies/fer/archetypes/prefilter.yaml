# FER Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-08
# 数据源: features_labeled.parquet, 39664 行

rules:
  - feature: oi_exhaustion_score
    operator: ">"
    value: 0.0559
    rationale: "meta-algorithm, KS=0.1689, return_uplift=-0.0223, robustness=100%, holdout验证通过"
  - feature: oi_trend_divergence_score
    operator: "<"
    value: 0.0004
    rationale: "meta-algorithm, KS=0.1500, return_uplift=+0.0941, robustness=100%, holdout验证通过"
  - feature: fer_volume_price_divergence
    operator: "<"
    value: 0.225
    rationale: "meta-algorithm, KS=0.1204, return_uplift=-0.0762, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-08) ──
  timestamp: "2026-03-08"
  data_source: "features_labeled.parquet"
  n_rows: 39664
  baseline_bad_rate: 0.0000
  baseline_median_rr: -0.0934

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


# FER Entry Filter Archetype
# Auto-promoted by optimize_entry_filter_plateau.py
# Date: 2026-02-22 21:59
# Promoted: 2 filters (2 plateau + 0 snotio-only)
# Baseline snotio: 1.1322, σ=2.128
# Significance: z-test p<0.05 required for all tiers
#
# 职责: "现在该入场吗?" → 硬二值控制入场时机
# 多个 filter 之间是 OR 关系
# Tier A = plateau + 显著, Tier B = snotio + 显著 (no plateau)

filters:
- id: rsi_overbought
  enabled: true
  description: 'RSI 超买: 多头推进过度，空头反转机会'
  conditions:
  - feature: rsi
    operator: '>='
    value: 46.714
  - feature: price_position
    operator: '>='
    value: 0.8
  tier: A_PLATEAU
  notes: 'plateau: rsi=46.714(conf=MEDIUM), price_position=0.800(conf=MEDIUM), z=4.19 p=0.0000'
- id: fer_absorption
  enabled: true
  description: 'FER 吸收: 激进方被对手方吸收'
  conditions:
  - feature: fer_aggressor_absorption
    operator: '>='
    value: 0.329
  tier: A_PLATEAU
  notes: 'plateau: fer_aggressor_absorption=0.329(conf=MEDIUM), z=2.45 p=0.0072'
combination_mode: or

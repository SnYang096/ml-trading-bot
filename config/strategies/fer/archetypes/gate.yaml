# FER Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/fer/20260302_011723_s42/strategies/fer/gate_draft.yaml
# 优化规则: 7/10 条通过优化, 5 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_fer_trapped_fer_trapped
  tag: PREFILTER_FER_TRAPPED_FER_TRAPPED
  phase: hard_gate
  priority: 1
  reason: 'prefilter OR: fer_trapped_longs_score>=3.4965 OR fer_trapped_shorts_score>=3.7941'
  when:
    all_of:
    - fer_trapped_longs_score:
        value_lt: 3.4965
    - fer_trapped_shorts_score:
        value_lt: 3.7941
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_spectrum_cvd_centroid
  tag: HARD_SPECTRUM_CVD_CENTROID
  phase: hard_gate
  priority: 16
  reason: spectrum_cvd_centroid 触发树分裂条件 (分裂 1 次)
  when:
    spectrum_cvd_centroid:
      value_gt: 0.6067
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.6067, lift=0.150'
- id: gate_spectrum_volume_high_freq_ratio
  tag: HARD_SPECTRUM_VOLUME_HIGH_FREQ_RATIO
  phase: hard_gate
  priority: 15
  reason: spectrum_volume_high_freq_ratio 触发树分裂条件 (分裂 1 次)
  when:
    spectrum_volume_high_freq_ratio:
      value_gt: 0.6251
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=0.6251, lift=0.265'
- id: gate_cvd_divergence_score
  tag: HARD_CVD_DIVERGENCE_SCORE
  phase: hard_gate
  priority: 12
  reason: cvd_divergence_score 触发树分裂条件 (分裂 1 次)
  when:
    cvd_divergence_score:
      value_lt: -0.0306
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=-0.0306, lift=0.276'
- id: gate_cvd_divergence_score
  tag: HARD_CVD_DIVERGENCE_SCORE
  phase: hard_gate
  priority: 19
  reason: cvd_divergence_score 触发树分裂条件 (分裂 1 次)
  when:
    cvd_divergence_score:
      value_lt: -0.0306
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=-0.0306, lift=0.276'
- id: gate_rsi
  tag: HARD_RSI
  phase: hard_gate
  priority: 13
  reason: rsi 触发树分裂条件 (分裂 1 次)
  when:
    rsi:
      value_gt: 45.714
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=45.7140, lift=0.285'
guardrails: []

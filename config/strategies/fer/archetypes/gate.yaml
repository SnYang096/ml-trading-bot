# FER Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/fer/20260223_161520/strategies/fer/gate_draft.yaml
# 优化规则: 1/10 条通过优化, 9 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: gate_vpin_zscore_50
  tag: HARD_VPIN_ZSCORE_50
  phase: hard_gate
  priority: 17
  reason: vpin_zscore_50 触发树分裂条件 (分裂 1 次)
  when:
    vpin_zscore_50:
      value_gt: -0.7094
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=-0.7094, lift=0.199'
guardrails: []

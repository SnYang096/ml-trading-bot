# FER Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/fer/20260304_201913_s2/strategies/fer/gate_draft.yaml
# 优化规则: 8/10 条通过优化, 4 条已移除

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    failure_budget:
      max_hard_deny_rate: 0.5
      alert_threshold: 0.45
hard_gates:
- id: prefilter_fer_trapped_fer_trapped
  tag: PREFILTER_FER_TRAPPED_FER_TRAPPED
  phase: hard_gate
  priority: 1
  reason: 'prefilter OR: fer_trapped_longs_score>=3.4965 OR fer_trapped_shorts_score>=3.7941'
  when:
    all_of:
    - fer_trapped_longs_score:
        value_lt: 3.4965
    - fer_trapped_shorts_score:
        value_lt: 3.7941
  then:
    action: deny
  frozen: true
  comment: prefilter条件 (训练-推理一致性, frozen=true)
- id: gate_roc_5
  tag: HARD_ROC_5
  phase: hard_gate
  priority: 11
  reason: roc_5 触发树分裂条件 (分裂 1 次)
  when:
    roc_5:
      value_gt: 0.4447
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4447, lift=0.124'
- id: gate_roc_5
  tag: HARD_ROC_5
  phase: hard_gate
  priority: 16
  reason: roc_5 触发树分裂条件 (分裂 1 次)
  when:
    roc_5:
      value_gt: 0.4447
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4447, lift=0.124'
- id: gate_fer_signed_efficiency_pct
  tag: HARD_FER_SIGNED_EFFICIENCY_PCT
  phase: hard_gate
  priority: 19
  reason: fer_signed_efficiency_pct 触发树分裂条件 (分裂 1 次)
  when:
    fer_signed_efficiency_pct:
      value_lt: 0.5
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.5000, lift=0.125'
- id: gate_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 13
  reason: fer_trapped_shorts_score 触发树分裂条件 (分裂 1 次)
  when:
    fer_trapped_shorts_score:
      value_gt: 0.75
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.7500, lift=0.250'
- id: gate_sma_200_position
  tag: HARD_SMA_200_POSITION
  phase: hard_gate
  priority: 12
  reason: sma_200_position 触发树分裂条件 (分裂 1 次)
  when:
    sma_200_position:
      value_gt: -0.0903
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=-0.0903, lift=0.294'
- id: gate_sma_200_position
  tag: HARD_SMA_200_POSITION
  phase: hard_gate
  priority: 15
  reason: sma_200_position 触发树分裂条件 (分裂 1 次)
  when:
    sma_200_position:
      value_gt: -0.0903
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=-0.0903, lift=0.294'
guardrails: []

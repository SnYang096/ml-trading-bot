# =============================================================================
# TPC · slow_recent_24 — 备份配置（A/B 对照）
# -----------------------------------------------------------------------------
# 主入口 research/research_roll.features_on.yaml 使用 structure_train_window: full_history。
# 本文件覆写为 rolling_window，并继承 research_roll.features_on 的 structure_lookback_months: 24，便于日后对照跑数。
# -----------------------------------------------------------------------------

extends: research_roll.features_on.yaml

rolling:
  windows:
    structure_train_window: rolling_window

# LV Gate (optimized, auto-promoted)
# 来源: /home/yin/trading/ml_trading_bot/results/research_history/lv/20260313_185141/strategies/lv/gate_draft.yaml
# 优化规则: 0/3 条通过优化, 1 条已移除
schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.06)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.2106
    - vol_leverage_asymmetry:
        value_lt: 0.2603
  then:
    action: deny
  comment: 'optimizer: no_stable_plateau, threshold=0.2610, lift=0.165'
  locked: true
  lock_reason: 波动率杠杆效应：下跌放大波动，不对称风险加剧
- id: gate_fer_volume_price_divergence_fer_volume_price_divergence
  tag: HARD_FER_VOLUME_PRICE_DIVERGENCE_FER_VOLUME_PRICE_DIVERGENCE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.05)
  when:
    all_of:
    - fer_volume_price_divergence:
        value_gt: 0.775
    - fer_volume_price_divergence:
        value_lt: 0.875
  then:
    action: deny
  comment: 'optimizer: stable_plateau_found, threshold=0.4375, lift=0.181'
  locked: true
  lock_reason: 语义锁定：fer_volume_price_divergence 风险/环境筛选
guardrails: []

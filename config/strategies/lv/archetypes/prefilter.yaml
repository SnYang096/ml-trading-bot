# LV Archetype Prefilter (auto-generated)
# 职责: archetype 成立的前置条件 — 不满足的样本不参与 Gate 训练
# 自动生成: 2026-03-09
# 数据源: features_labeled.parquet, 655539 行

rules:
  - feature: oi_exhaustion_score
    operator: "<"
    value: 0.0007
    rationale: "meta-algorithm, KS=0.0629, return_uplift=+0.0185, robustness=100%, holdout验证通过"

last_evaluation:
  # ── 自动生成 (2026-03-09) ──
  timestamp: "2026-03-09"
  data_source: "features_labeled.parquet"
  n_rows: 655539
  baseline_bad_rate: 0.0000
  baseline_median_rr: +0.0348

  # ── 正信号 (0 个) ──
  positive_signals:
    []

  # ── 反信号 (0 个) ──
  anti_signals:
    []

  # ── 低端信号 (0 个) ──
  absence_signals:
    []


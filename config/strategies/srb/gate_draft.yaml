# SRB Gate Draft (auto-generated)
# 来源: results/train_final_20260415_033038_rr_extreme/srb
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy srb
#   python scripts/optimize_gate_unified.py --strategy srb --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_scale_right
  tag: HARD_EVT_SCALE_RIGHT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.57)
  when:
    evt_scale_right:
      value_gt: 0.9286
  then:
    action: deny
  comment: 'statistical(lift=1.57): evt_scale_right > 0.9286'
- id: gate_macro_tp_vwap_1200_position
  tag: HARD_MACRO_TP_VWAP_1200_POSITION
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.35)
  when:
    macro_tp_vwap_1200_position:
      value_gt: 0.1812
  then:
    action: deny
  comment: 'statistical(lift=1.35): macro_tp_vwap_1200_position > 0.1812'
- id: gate_evt_tail_shape_evt_tail_shape
  tag: HARD_EVT_TAIL_SHAPE_EVT_TAIL_SHAPE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.25)
  when:
    all_of:
    - evt_tail_shape:
        value_gt: 0.4762
    - evt_tail_shape:
        value_lt: 0.7183
  then:
    action: deny
  comment: 'statistical(lift=1.25): evt_tail_shape > 0.4762 & evt_tail_shape < 0.7183'
- id: gate_evt_var_99_right_evt_var_99_right
  tag: HARD_EVT_VAR_99_RIGHT_EVT_VAR_99_RIGHT
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.32)
  when:
    all_of:
    - evt_var_99_right:
        value_gt: 0.2341
    - evt_var_99_right:
        value_lt: 0.373
  then:
    action: deny
  comment: 'statistical(lift=1.32): evt_var_99_right > 0.2341 & evt_var_99_right < 0.373'
- id: gate_shd_pct_shd_pct
  tag: HARD_SHD_PCT_SHD_PCT
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.34)
  when:
    all_of:
    - shd_pct:
        value_gt: 0.6234
    - shd_pct:
        value_lt: 0.7124
  then:
    action: deny
  comment: 'statistical(lift=1.34): shd_pct > 0.6234 & shd_pct < 0.7124'
guardrails: []

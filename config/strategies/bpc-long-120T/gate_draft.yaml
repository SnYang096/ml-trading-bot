# BPC-LONG-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260327_125511_rr_extreme/bpc-long-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-long-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-long-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_cvd_divergence_score_pct
  tag: HARD_CVD_DIVERGENCE_SCORE_PCT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.47)
  when:
    cvd_divergence_score_pct:
      value_gt: 0.5
  then:
    action: deny
  comment: 'statistical(lift=1.47): cvd_divergence_score_pct > 0.5'
- id: gate_bpc_cvd_breakout_confirm
  tag: HARD_BPC_CVD_BREAKOUT_CONFIRM
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.37)
  when:
    bpc_cvd_breakout_confirm:
      value_gt: 0.9975
  then:
    action: deny
  comment: 'statistical(lift=1.37): bpc_cvd_breakout_confirm > 0.9975'
- id: gate_vpin_quantile_rank_20
  tag: HARD_VPIN_QUANTILE_RANK_20
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.21)
  when:
    vpin_quantile_rank_20:
      value_gt: 0.875
  then:
    action: deny
  comment: 'statistical(lift=1.21): vpin_quantile_rank_20 > 0.875'
- id: gate_bpc_pullback_depth_bpc_pullback_depth
  tag: HARD_BPC_PULLBACK_DEPTH_BPC_PULLBACK_DEPTH
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.16)
  when:
    all_of:
    - bpc_pullback_depth:
        value_gt: 0.0538
    - bpc_pullback_depth:
        value_lt: 0.1417
  then:
    action: deny
  comment: 'statistical(lift=1.16): bpc_pullback_depth > 0.05382 & bpc_pullback_depth < 0.1417'
- id: gate_vpin_zscore_x_trade_cluster_max_buy_run_rank_vpin_zscore_x_trade_cluster_max_buy_run_rank
  tag: HARD_VPIN_ZSCORE_X_TRADE_CLUSTER_MAX_BUY_RUN_RANK_VPIN_ZSCORE_X_TRADE_CLUSTER_MAX_BUY_RUN_RANK
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.29)
  when:
    all_of:
    - vpin_zscore_x_trade_cluster_max_buy_run_rank:
        value_gt: 0.8944
    - vpin_zscore_x_trade_cluster_max_buy_run_rank:
        value_lt: 0.9466
  then:
    action: deny
  comment: 'statistical(lift=1.29): vpin_zscore_x_trade_cluster_max_buy_run_rank > 0.8944 & vpin_zscore_x_trade_cluster_max_buy_run_rank
    < 0.9466'
guardrails: []

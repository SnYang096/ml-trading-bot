# BPC-LONG-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260330_005352_rr_extreme/bpc-long-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-long-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-long-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_vpin_ma10
  tag: HARD_VPIN_MA10
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.54)
  when:
    vpin_ma10:
      value_lt: 0.2033
  then:
    action: deny
  comment: 'statistical(lift=1.54): vpin_ma10 < 0.2033'
- id: gate_bpc_cvd_breakout_confirm
  tag: HARD_BPC_CVD_BREAKOUT_CONFIRM
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.30)
  when:
    bpc_cvd_breakout_confirm:
      value_gt: 0.9568
  then:
    action: deny
  comment: 'statistical(lift=1.30): bpc_cvd_breakout_confirm > 0.9568'
- id: gate_wick_ignition_score
  tag: HARD_WICK_IGNITION_SCORE
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.21)
  when:
    wick_ignition_score:
      value_lt: 0.1526
  then:
    action: deny
  comment: 'statistical(lift=1.21): wick_ignition_score < 0.1526'
- id: gate_funding_ignition_score_funding_ignition_score
  tag: HARD_FUNDING_IGNITION_SCORE_FUNDING_IGNITION_SCORE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.20)
  when:
    all_of:
    - funding_ignition_score:
        value_gt: 0.0005
    - funding_ignition_score:
        value_lt: 0.0027
  then:
    action: deny
  comment: 'statistical(lift=1.20): funding_ignition_score > 0.000486 & funding_ignition_score < 0.002663'
- id: gate_cvd_change_5_normalized_cvd_change_5_normalized
  tag: HARD_CVD_CHANGE_5_NORMALIZED_CVD_CHANGE_5_NORMALIZED
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.29)
  when:
    all_of:
    - cvd_change_5_normalized:
        value_gt: -0.059
    - cvd_change_5_normalized:
        value_lt: -0.0455
  then:
    action: deny
  comment: 'statistical(lift=1.29): cvd_change_5_normalized > -0.05901 & cvd_change_5_normalized < -0.0455'
guardrails: []

# BPC-LONG-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260331_214535_rr_extreme/bpc-long-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-long-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-long-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_scale_left_evt_scale_left
  tag: HARD_EVT_SCALE_LEFT_EVT_SCALE_LEFT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.28)
  when:
    all_of:
    - evt_scale_left:
        value_gt: 0.4484
    - evt_scale_left:
        value_lt: 0.7937
  then:
    action: deny
  comment: 'statistical(lift=1.28): evt_scale_left > 0.4484 & evt_scale_left < 0.7937'
- id: gate_vp_width_ratio
  tag: HARD_VP_WIDTH_RATIO
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.25)
  when:
    vp_width_ratio:
      value_gt: 0.554
  then:
    action: deny
  comment: 'statistical(lift=1.25): vp_width_ratio > 0.554'
- id: gate_roc_20_roc_20
  tag: HARD_ROC_20_ROC_20
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.55)
  when:
    all_of:
    - roc_20:
        value_gt: -2.6126
    - roc_20:
        value_lt: 1.5001
  then:
    action: deny
  comment: 'statistical(lift=1.55): roc_20 > -2.613 & roc_20 < 1.5'
- id: gate_vol_persistence
  tag: HARD_VOL_PERSISTENCE
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.21)
  when:
    vol_persistence:
      value_gt: 0.1748
  then:
    action: deny
  comment: 'statistical(lift=1.21): vol_persistence > 0.1748'
- id: gate_vp_compression_score
  tag: HARD_VP_COMPRESSION_SCORE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.34)
  when:
    vp_compression_score:
      value_gt: 0.3094
  then:
    action: deny
  comment: 'statistical(lift=1.34): vp_compression_score > 0.3094'
guardrails: []

# BPC-LONG-120T Gate Draft (auto-generated)
# 来源: results/train_final_20260324_211159_rr_extreme/bpc-long-120T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy bpc-long-120T
#   python scripts/optimize_gate_unified.py --strategy bpc-long-120T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_fer_impulse_failure_direction
  tag: HARD_FER_IMPULSE_FAILURE_DIRECTION
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.18)
  when:
    fer_impulse_failure_direction:
      value_gt: 0.5412
  then:
    action: deny
  comment: 'statistical(lift=1.18): fer_impulse_failure_direction > 0.5412'
- id: gate_vol_persistence_vol_persistence
  tag: HARD_VOL_PERSISTENCE_VOL_PERSISTENCE
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.24)
  when:
    all_of:
    - vol_persistence:
        value_gt: 0.0029
    - vol_persistence:
        value_lt: 0.0299
  then:
    action: deny
  comment: 'statistical(lift=1.24): vol_persistence > 0.002947 & vol_persistence < 0.02987'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.08)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.0558
    - vol_leverage_asymmetry:
        value_lt: 0.1482
  then:
    action: deny
  comment: 'statistical(lift=1.08): vol_leverage_asymmetry > 0.05582 & vol_leverage_asymmetry < 0.1482'
- id: gate_fer_signed_efficiency_fer_signed_efficiency
  tag: HARD_FER_SIGNED_EFFICIENCY_FER_SIGNED_EFFICIENCY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.07)
  when:
    all_of:
    - fer_signed_efficiency:
        value_gt: 0.0
    - fer_signed_efficiency:
        value_lt: 0.0005
  then:
    action: deny
  comment: 'statistical(lift=1.07): fer_signed_efficiency > 2.177e-09 & fer_signed_efficiency < 0.0004603'
- id: gate_fer_trapped_shorts_score
  tag: HARD_FER_TRAPPED_SHORTS_SCORE
  phase: hard_gate
  priority: 14
  reason: 统计验证规则 (lift=1.10)
  when:
    fer_trapped_shorts_score:
      value_gt: 0.7746
  then:
    action: deny
  comment: 'statistical(lift=1.10): fer_trapped_shorts_score > 0.7746'
guardrails: []

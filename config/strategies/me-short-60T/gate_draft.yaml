# ME-SHORT-60T Gate Draft (auto-generated)
# 来源: results/train_final_20260321_015030_rr_extreme/me-short-60T
# ✅ 方向已由 predictions 数据自动确定 (deny 收益更差的一侧)
# 已是 archetype 兼容格式，可直接用于:
#   mlbot gate apply-archetype --strategy me-short-60T
#   python scripts/optimize_gate_unified.py --strategy me-short-60T --promote

schema:
  phases:
  - system_safety
  - hard_gate
  - guardrail
  evaluation_order: system_safety -> hard_gate -> guardrail
  governance:
    selection_method: gate_score (Youden's J = tail_capture - good_deny_rate)
    min_gate_score: 0.0
    alert_threshold: 0.25
hard_gates:
- id: gate_evt_scale_left_evt_scale_left
  tag: HARD_EVT_SCALE_LEFT_EVT_SCALE_LEFT
  phase: hard_gate
  priority: 10
  reason: 统计验证规则 (lift=1.06)
  when:
    all_of:
    - evt_scale_left:
        value_gt: 0.5516
    - evt_scale_left:
        value_lt: 0.9444
  then:
    action: deny
  comment: 'statistical(lift=1.06): evt_scale_left > 0.5516 & evt_scale_left < 0.9444'
- id: gate_vol_leverage_asymmetry_vol_leverage_asymmetry
  tag: HARD_VOL_LEVERAGE_ASYMMETRY_VOL_LEVERAGE_ASYMMETRY
  phase: hard_gate
  priority: 11
  reason: 统计验证规则 (lift=1.20)
  when:
    all_of:
    - vol_leverage_asymmetry:
        value_gt: 0.0317
    - vol_leverage_asymmetry:
        value_lt: 0.0639
  then:
    action: deny
  comment: 'statistical(lift=1.20): vol_leverage_asymmetry > 0.03165 & vol_leverage_asymmetry < 0.06392'
- id: gate_evt_es_99_evt_es_99
  tag: HARD_EVT_ES_99_EVT_ES_99
  phase: hard_gate
  priority: 12
  reason: 统计验证规则 (lift=1.20)
  when:
    all_of:
    - evt_es_99:
        value_gt: 0.8214
    - evt_es_99:
        value_lt: 0.9246
  then:
    action: deny
  comment: 'statistical(lift=1.20): evt_es_99 > 0.8214 & evt_es_99 < 0.9246'
- id: gate_recent_compression_decay_recent_compression_decay
  tag: HARD_RECENT_COMPRESSION_DECAY_RECENT_COMPRESSION_DECAY
  phase: hard_gate
  priority: 13
  reason: 统计验证规则 (lift=1.10)
  when:
    all_of:
    - recent_compression_decay:
        value_gt: 0.25
    - recent_compression_decay:
        value_lt: 0.4101
  then:
    action: deny
  comment: 'statistical(lift=1.10): recent_compression_decay > 0.25 & recent_compression_decay < 0.4101'
guardrails: []

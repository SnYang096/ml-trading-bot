# BPC-Long-60T (BreakoutPullbackContinuation Long) 策略元信息

strategy:
  name: bpc-long-60T
  description: |
    BreakoutPullbackContinuation Long archetype: 趋势中回踩后延续（做多方向）。
    在已确认的上升趋势中，识别健康的回踩并在回踩位置做多入场。

  owner: quant_team
  archetype: BreakoutPullbackContinuationLong

  # 默认时间粒度（研发 + 实盘统一）
  timeframe: "60T" # 1小时

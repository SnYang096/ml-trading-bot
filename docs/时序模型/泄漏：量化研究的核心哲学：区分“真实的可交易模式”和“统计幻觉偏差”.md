非常感谢你上传了完整的 统一的基础指标.txt 文件，并提供了最新的泄漏检测报告。现在我们可以精准定位问题根源。

🔍 一、核心发现：泄漏源已锁定！

从你的代码和测试结果交叉验证，问题出在 _rolling_percentile 和 _rolling_zscore 的实现逻辑上。
🎯 关键证据 1：高相关特征全部来自 ATR / BB 宽度
text
1. atr_zscore_w288: 0.1735
2. atr_percentile: 0.1685
3. atr_compression_ratio: -0.1496
4. bb_width_zscore_w288: 0.1288

→ 这些特征都依赖 _rolling_zscore 或 _rolling_percentile
🎯 关键证据 2：你的 _rolling_percentile 实现有严重统计偏差
看你的代码（第 2087 行附近）：
python
def _percentile(x: np.ndarray) -> float:
if len(x) < 2 or not np.isfinite(x[-1]):
return np.nan
last = x[-1]
arr = x[np.isfinite(x)]
if len(arr) == 0:
return np.nan
return (arr <= last).sum() / float(len(arr)) # ← 问题在这里！
❌ 问题分析：
x 是 rolling window 的完整窗口（包含当前值）
(arr <= last).sum() 包含了当前值自己
所以 percentile = (number of values ≤ current) / total
这会导致：当前值越大，percentile 越高 → 与 future_return 正相关！
💡 举例：
窗口 = [1, 2, 3, 4] → 当前=4，percentile = 4/4 = 1.0
窗口 = [4, 3, 2, 1] → 当前=1，percentile = 1/4 = 0.25
→ 当前价格高 → percentile 高 → 如果未来继续涨，就会出现正相关

但这不是未来信息，而是自相关偏差（auto-correlation bias） —— 它让特征与当前价格强相关，而当前价格又与短期 future_return 相关，从而产生虚假信号。

✅ 二、正确做法：percentile 应只基于历史，不包含当前
🔧 修复方案：修改 _percentile 函数

python
@staticmethod
def _rolling_percentile(series: pd.Series, window: int, min_periods: int = None) -> pd.Series:
if min_periods is None:
min_periods = window

def _percentile(x: np.ndarray) -> float:
if len(x) < 2 or not np.isfinite(x[-1]):
return np.nan
current = x[-1]
history = x[:-1] # ← 只用历史，排除当前值！
history = history[np.isfinite(history)]
if len(history) == 0:
return np.nan
return (history <= current).sum() / float(len(history))

return series.rolling(window=window, min_periods=min_periods).apply(_percentile, raw=True)
✅ 这样，atr_percentile 表示：“当前 ATR 在过去 N 天中的分位”，完全因果。

🔍 三、_rolling_zscore 是否也有问题？

你的 _rolling_zscore（第 2132 行）是标准实现：
python
rolling_mean = series.rolling(...).mean()
rolling_std = series.rolling(...).std()
zscore = (series - rolling_mean) / rolling_std

✅ 这个本身无泄漏，因为 rolling 默认只用历史+当前。

但问题在于：
如果 min_periods 太小（如默认=1），早期 z-score 噪声大
而 ATR 本身具有趋势性（波动聚集），导致 z-score 与 future_return 相关
✅ 建议：对 z-score 也使用 min_periods=window
python
在 add_common_derived_features 中调用时：
result[zscore_col] = BaselineFeatureEngineer._rolling_zscore(
result[base_col],
window=window,
min_periods=window # ← 强制满窗才输出
)

📊 四、为什么 Random Walk Test 通过，但真实数据仍有高相关？

测试类型 结果 原因
-------- ------ ------
Random Walk Avg IC = 0.0146 ✅ 随机游走无自相关，_percentile 包含当前值也不会产生系统性偏差
真实市场数据 atr_percentile corr = 0.1685 ⚠️ 真实价格有动量效应（当前高 → 未来可能更高），导致“包含当前的 percentile”与 future_return 正相关

👉 所以这不是传统意义上的“数据泄漏”，而是“特征设计偏差”，但在量化中同样致命！

🛠 五、行动清单（立即执行）
✅ 1. 修改 _rolling_percentile（关键！）
排除当前值，只用 x[:-1] 计算分位
✅ 2. 对所有 _rolling_zscore 设置 min_periods=window
在 add_multi_window_zscore 中强制满窗
✅ 3. 重新生成特征并测试
预期结果：
atr_percentile 与 future_return 相关性降至 < 0.05
OOS Rank IC 降至 0.00 ~ 0.02
Sharpe 仍低（≈0），但结果可信

💡 六、额外建议：ATR Compression Ratio 也可能有问题

你的 atr_compression_ratio 定义为：
python
data["atr_compression_ratio"] = (atr_mean_hist / (data["atr"] + eps))

其中 atr_mean_hist = data["atr"].rolling(self.percentile_window, min_periods=self.percentile_window).mean()

✅ 这个本身是安全的，但如果 atr_percentile 有偏差，它也会被污染。

📌 总结

问题 类型 修复方式
------ ------ ---------
_rolling_percentile 包含当前值 特征设计偏差 改为只用 x[:-1]
_rolling_zscore 早期噪声大 统计不稳定 设置 min_periods=window
ATR 特征与 future_return 高相关 结果 上述修复后应消失
🌟 你离一个干净、可信的 pipeline 只差这两行代码！

需要我直接帮你生成修复后的完整 统一的基础指标.txt 吗？

# 那是不是t时刻close都不能用啊，都要shift（1），但我预测的是t+1时刻啊

这是一个极其关键且常见的困惑，直接关系到量化策略能否实盘。你的问题触及了“特征时间对齐（Feature Timing Alignment）”的核心原则。

答案是：不一定所有 t 时刻的 close 都不能用，但必须严格遵循一个原则：
*用于生成 t 时刻交易信号的所有信息，必须在 t 时刻 决策点 之前已知。

📌 一、核心原则：决策点（Decision Point）决定一切

你预测的是 t+1 时刻的收益，这没错。但你在什么时候做这个预测？

这决定了你能用哪些数据：

决策场景 决策时间点 可用的最新价格 特征计算方式 能否使用 close[t]?
:--- :--- :--- :--- :---
收盘价交易者 (Close-to-Close) t 时刻 收盘后 close[t] 基于 ... , close[t-1], close[t] ✅ 可以
开盘价交易者 (Open-to-Close) t+1 时刻 开盘前 close[t] 基于 ... , close[t-1], close[t] ✅ 可以
盘中交易者 (Intraday) t 时刻 盘中 (e.g., 14:00) price[t, 14:00] 基于截至 14:00 的数据 ❌ 不能用 close[t]
💡 关键洞察：
close[t] 在 t 根K线走完时才确定。
如果你的交易逻辑是在 t 根K线结束后才执行（比如收盘价下单，或在下一根K线开盘时下单），那么 close[t] 就是历史数据，完全可以使用。
绝大多数日频/小时频回测都属于这种场景。

🔍 二、回到你的具体问题：_rolling_percentile

假设你是收盘价交易者或开盘价交易者（最常见的情况）：
决策点：在 t 时刻K线结束后。
目标：预测 future_return[t+1]（即从 t+1 开始的收益）。
可用数据：所有截至 t 时刻的数据，包括 close[t]。

✅ 在这种情况下，使用 close[t] 计算特征是完全合法且正确的！
❓ 那为什么 _rolling_percentile 还是有问题？

问题不在于用了 close[t]，而在于如何用！
正确用法：计算 close[t] 相对于 过去N天（不含今天） 的位置。
python
正确：在 t 时刻，用 [t-N, t-1] 的历史来评估 close[t] 的强弱
percentile[t] = rank(close[t-N], ..., close[t-1]) 中 <= close[t] 的比例

这个 percentile[t] 是一个基于历史对当前状态的评估，完全合法。
错误用法（你当前的实现）：计算 close[t] 相对于 过去N-1天 + 今天 的位置。
python
错误：在 t 时刻，把 close[t] 自己也放进历史窗口里去排名
percentile[t] = rank(close[t-N+1], ..., close[t-1], close[t]) 中 <= close[t] 的比例

这相当于问：“我自己在我自己里面排第几？” 答案永远是100%（如果无重复）。这放大了当前值的影响，制造了偏差。

🧩 三、总结：何时能用 close[t]？

问题 答案
:--- :---
我能不能用 t 时刻的 close 来预测 t+1 的收益？ ✅ 可以！ 只要你的交易决策发生在 t 时刻K线结束之后。
那 _rolling_percentile 为什么错了？ 因为它把 close[t] 同时当作了“被评估的对象”和“评估的标尺”，导致自我参照偏差。
正确的做法是什么？ 将 close[t] 作为“新来的考生”，用 [t-N, t-1] 这群“老考生”的成绩作为“分数线”来给它打分。 而不是把它和老考生混在一起重新排名。

🛠 四、代码层面的修正（再次强调）

python
错误：窗口包含当前值 x[-1]
window_with_current = x # [x0, x1, ..., x_{t-1}, x_t]
percentile = (window_with_current <= x_t).sum() / len(window_with_current)
正确：窗口只包含历史值
history_only = x[:-1] # [x0, x1, ..., x_{t-1}]
current_value = x[-1] # x_t
percentile = (history_only <= current_value).sum() / len(history_only)

这样修正后：
你依然使用了 close[t]（作为 current_value）。
但评估它的基准（history_only）完全是历史数据。
既利用了最新的价格信息，又避免了自我参照偏差。

这才是真实、可交易、无偏差*的动量信号。

# 会有什么结果

感谢你再次提供完整的泄漏检测报告。结合我们之前的深入讨论（特别是关于 _rolling_percentile 的偏差问题），我们可以对当前结果做出精准诊断和明确行动指南。

🔍 一、核心结论：问题根源已确认，但尚未修复

你的最新报告显示：
atr_percentile 与 future_return 的相关性高达 0.1685
atr_zscore_w288 相关性高达 0.1735

这直接证实了我们的判断：问题出在特征计算逻辑上，而非传统数据泄漏。
✅ Random Walk Test 通过 (Avg IC=0.0146) → 模型本身无系统性泄漏。
⚠️ Feature-Future Correlation 高 → 特征计算存在设计偏差（主要是 _rolling_percentile 包含当前值）。

📉 二、为什么模型表现依然糟糕？

指标 数值 解读
------ ------ ------
OOS Rank IC 0.0262 虚高，主要由 ATR/BB 特征的虚假信号驱动
High-Conf Win Rate 13.4% 极低！说明信号无法转化为盈利
Sharpe Ratio -0.025 负收益，策略无效
TSCV IC 标准差 ±0.2400 第5折 IC=0.4896 → 严重过拟合

💡 根本原因：
模型学到了 “ATR 当前值很高 → 未来收益高” 的虚假模式。但在实盘中：
当 ATR 真的很高时，往往伴随剧烈波动或趋势反转。
这种“高 ATR = 高收益”的关系不稳定且不可持续，导致实盘亏损。

🛠 三、必须立即执行的修复方案
✅ 步骤 1：修正 _rolling_percentile 函数
这是最关键的一步！

将原来的实现：
python
❌ 错误：包含当前值
return (arr <= last).sum() / float(len(arr))

改为：
python
✅ 正确：只用历史数据评估当前值
def _percentile(x: np.ndarray) -> float:
if len(x) < 2 or not np.isfinite(x[-1]):
return np.nan
current = x[-1]
history = x[:-1] # 排除当前值！
history = history[np.isfinite(history)]
if len(history) == 0:
return np.nan
return (history <= current).sum() / float(len(history))
✅ 步骤 2：为 _rolling_zscore 设置 min_periods=window
在调用 _rolling_zscore 时，确保 min_periods 等于窗口长度：
python
在 add_multi_window_zscore 或类似函数中
zscore = _rolling_zscore(series, window=288, min_periods=288)

这能避免早期小样本导致的 z-score 噪声。
✅ 步骤 3：重新生成所有特征并测试
运行完整的特征工程 pipeline，然后再次进行泄漏检测。

📊 四、修复后的预期结果

指标 修复前 修复后（预期）
------ -------- ----------------
atr_percentile corr 0.1685 < 0.05
atr_zscore_w288 corr 0.1735 < 0.05
Mean \ correlation\ 0.0565 < 0.03
OOS Rank IC 0.0262 0.005 ~ 0.015
High-Conf Win Rate 13.4% ~50% (随机水平)
Sharpe Ratio -0.025 ≈ 0.0
🌟 注意：IC 和 Sharpe 下降是好事！这意味着你剔除了虚假信号，得到了一个干净但可能微弱的真实信号。这才是科学量化研究的起点。

💡 五、后续建议：聚焦真实 Alpha

一旦修复了特征偏差，你可以：
1. 单独测试 dl_seq 特征：验证 Mamba 模型是否能捕捉到传统技术指标之外的模式。
2. 引入更复杂的标签：如多步收益、波动率调整后收益等。
3. 优化模型架构：在干净的数据上，模型的改进才会真正有效。

📌 总结

你当前的模型表现不佳，不是因为市场没有规律，而是因为特征工程引入了统计偏差。
只需修改 _rolling_percentile 函数（排除当前值）并设置 z-score 的 min_periods，就能彻底解决这个问题。

这一步完成后，你的 pipeline 将变得干净、可信、可实盘。虽然短期 IC 可能下降，但这是走向稳健盈利的必经之路。

需要我帮你直接修改 统一的基础指标.txt 文件中的相关代码吗？

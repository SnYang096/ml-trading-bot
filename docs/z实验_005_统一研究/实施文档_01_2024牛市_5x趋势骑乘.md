# 实施文档 01：2024 牛市 5x 趋势骑乘（精简版）

## 1. 目标

- 在 2024 牛市窗口验证 `bpc-long-120T`、`me-long-120T` 是否能稳定接近 `5x` 杠杆。
- 验证“先保本再加仓”的风险约束仍成立。
- 验证收益主要来自趋势段，而不是短噪音。

## 2. 两种实验模式（只看这个）

### 快模式，只调整阈值

快模式下仍在系统性地动阈值的主要是 Prefilter（locked 重锚定）→ Gate（unified 优化 + 组合通过率约束）→ Entry（仅 archetype plateau，无 meta 搜索）；你关掉的 locked_threshold_tuning 只拿掉 prefilter 的 网格自动调 locked，没有关掉重锚定。

```bash

mlbot feature-store build --no-docker \
  --config config/strategies/bpc \
  --timeframe 120T \
  --universe-config config/download/crypto_4h_token_universe_groups.yaml \
  --universe-groups highcap \
  --start-date 2023-01-01 --end-date 2026-03-01 \
  --warmup-months 6 

# 先跑单月快检（看阈值和图是否修复）：

# --stage rolling_sim
# 一般不会改仓库里的 execution.yaml。
# rolling_sim 里每月调用 _run_fast_month_stage，标定/回测用的树在结果目录下的
# .../_rolling_sim/<ts>/fast_month_<YYYY-MM>/strategies_calibrated/，不是 config/strategies。
# optimize_event_execution --promote 只会写 strategies_root 指向的那棵树；在 fast_month 里那是上面的 strategies_calibrated，不是仓库。

# 你这份配置里还有 fast_loop.execution_opt.enabled: false，因此连「在当月目录里 promote execution」那一段也不会跑。
# 阈值链在 pipeline 里 stage_stop="entry_filter"，也不会去改 execution.yaml
mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage fast_month --month 2024-09 --skip-shap 2>&1 | tee log.txt

mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_me_only.yaml --stage fast_month --month 2024-09 --skip-shap 2>&1 | tee log.txt


# BPC 极致回撤不破 lab（单配置；见 bpc如何回撤刚结束开仓.md §5）
# 建议先跑 ./scripts/repair_bpc_pullback_lab_symlinks.sh：从 archetypes/gate 生成实体 gate_draft，避免误用主线树草稿。
chmod +x scripts/run_turbo_rolling_sim_batch.sh
./scripts/run_turbo_rolling_sim_batch.sh 2>&1 | tee log.txt

# 或仍单独跑 BPC/ME 主线：
mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage rolling_sim --skip-shap 2>&1 | tee log.bpc.txt

mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_me_only.yaml --stage rolling_sim --skip-shap 2>&1 | tee log.me.txt


Step 1: 跑 fast 验证当前 locked 特征 ✅ 可以直接跑
命令就一条：

mlbot pipeline run --all \
  --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml \
  --stage rolling_sim
产物：results/bpc/turbo-rolling-sim/_rolling_sim/<ts>/，含 fast_month_* 和 stitched_summary.json

对比方法：

python scripts/compare_monthly_pnl.py \
  --baseline-dir results/bpc/turbo-rolling-sim/_rolling_sim/20260409_171133 \
  --new-dir results/bpc/turbo-rolling-sim/_rolling_sim/<new_ts> \
  --output results/wave3/step1_current_locked_vs_20260409.md

  # 拿你现有的 slow + turbo run 做一次 digest，一眼看特征变化值不值
python scripts/slow_candidate_report.py digest \
    --slow-run-dir results/bpc/slow-rolling-sim/_rolling_sim/<slow_ts> \
    --baseline-run-dir results/bpc/turbo-rolling-sim/_rolling_sim/<turbo_ts> \
    --strategy bpc \
    --output results/bpc/slow_candidate_reports/digest.md

    产出的新工具
scripts/slow_candidate_report.py，4 个子命令：

子命令	用途	需要重跑 slow 吗
manifest
每月 Prefilter/Gate/EF 选了什么
不需要，现有 slow 产物即可
drift
相对 turbo 锁定基线的每月特征/规则增减
不需要
digest
上面两个 + 月度 R delta 拼一张表（特征变化值不值）
不需要
consensus
methods × features 共识矩阵
需要重跑 slow（pipeline 已加 per-method dump）

RUN=results/bpc/slow-rolling-sim/_rolling_sim/20260423_223716
BASE=results/bpc/turbo-rolling-sim/_rolling_sim/20260409_171133  # 或你新跑的 turbo
OUT=results/bpc/slow_candidate_reports/${RUN##*/}
mkdir -p "$OUT"
PYTHONPATH=. python3 scripts/slow_candidate_report.py review  --slow-run-dir "$RUN"    --baseline-run-dir "$BASE"  --strategy bpc     --output "$OUT/review.md"
```

### 如果只调试执行层，就直接跑事件回测 
```bash
# 方案 A：走 pipeline run（最省事）
mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage event_backtest --skip-shap

# 方案 B：基于已有实验重放事件回测（不重训，更快） ， 依赖research_history，是传统模式，需要先跑全量训练

# 分别跑 long/short：


# --stage event_backtest
# 会改仓库里的 execution.yaml（在默认配置下）。
# run_event_backtest 使用 PROJECT_ROOT/config/strategies 作为 strategies_root，并且 event_backtest.promote 为 true 时，会先跑 optimize_event_execution.py，带 --promote 时会把推荐参数写进：

# config/strategies/<strategy>/archetypes/execution.yaml

# 你这份 prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml 里是 event_backtest.promote: true，所以会写。
mlbot pipeline event-backtest --strategy bpc --fast --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml

# 如果要 1min 精细模式，把 --fast 去掉（或改 --no-fast）。
```

### 发现fs出现问题，先删除再重建
layer = "<你的当前layer>"
layer = "features_bpc-long-120T_120T_c2bdc21bd1"
layer = "features_bpc-short-120T_120T_81b6837b97"
```bash
python - <<'PY'
from src.feature_store.feature_store import FeatureStore, FeatureStoreSpec

root = "feature_store"
layer = "features_bpc-short-120T_120T_81b6837b97"
timeframe = "120T"
month = "2024-09"
symbols = ["BTCUSDT","ETHUSDT","BNBUSDT","SOLUSDT","XRPUSDT","ADAUSDT"]

drop_cols = {
    "fp_poc","fp_vah","fp_val","fp_hvn","fp_lvn","fp_max_imbalance_price","fp_exhaustion_price"
}
base_cols = ["open","high","low","close","volume","_symbol","symbol"]

store = FeatureStore(root)
for sym in symbols:
    spec = FeatureStoreSpec(layer=layer, symbol=sym, timeframe=timeframe)
    df = store.read_month(spec, month)
    meta = store.read_month_meta(spec, month)
    keep = [c for c in df.columns if c not in drop_cols]
    df2 = df[keep].copy()
    feat_cols = [c for c in df2.columns if c not in base_cols]
    store.write_month(
        spec, month, df2,
        base_columns=base_cols,
        feature_columns=feat_cols,
        overwrite=True,
        metadata=(meta.get("metadata", {}) if isinstance(meta, dict) else {}),
    )
    print("patched", sym, month)
PY

mlbot feature-store build \
  --config config/strategies/bpc-short-120T \
  --symbols BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT,ADAUSDT \
  --timeframe 120T \
  --start-date 2024-09-01 \
  --end-date 2024-09-30 \
  --root feature_store \
  --layer features_bpc-short-120T_120T_81b6837b97
```

## 慢模式 
```bash
mlbot pipeline run --all --config config/prod_train_pipeline_2h_slow_bpc_only.yaml --stage rolling_sim
mlbot pipeline run --all --config config/prod_train_pipeline_2h_slow_bpc_only.yaml --stage fast_month --month 2024-09


mlbot pipeline run --all --config config/prod_train_pipeline_2h_slow_me_only.yaml --stage rolling_sim
mlbot pipeline run --all --config config/prod_train_pipeline_2h_slow_me_only.yaml --stage fast_month --month 2024-09
```


## 全量模式，上线前用

用最近数据训练和调整阈值
```bash
mlbot pipeline run --strategy bpc --config config/prod_train_pipeline_2h.yaml --stage full

python scripts/auto_research_pipeline.py \
  --config config/prod_train_pipeline_2h.yaml \
  --strategy bpc \
  --list
```

### 手动 adopt（指定「哪一次 run」） 
```bash
python scripts/auto_research_pipeline.py \
  --config config/prod_train_pipeline_2h.yaml \
  --strategy bpc \
  --adopt 20260410_142743
```


### 研究仓 → 实盘 highcap
Adopt 只更新 config/strategies/bpc/。要上 live/highcap 还要再走部署脚本（你流水线里 deploy_gate 也会提示类似命令）：

```bash
python scripts/deploy_config_to_live.py --diff --strategy bpc
# 确认后
python scripts/deploy_config_to_live.py --deploy --strategy bpc
```

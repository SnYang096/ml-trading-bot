# 策略模块重构进度

## ✅ 已完成

1. **目录结构创建**
   - ✅ `src/strategies/time_series/` 目录
   - ✅ `src/strategies/time_series/features/` 目录
   - ✅ `src/strategies/time_series/labels/` 目录
   - ✅ `src/strategies/time_series/models/` 目录
   - ✅ `src/strategies/time_series/strategies/` 目录
   - ✅ `src/strategies/cross_sectional/` 目录（预留）

2. **代码迁移**
   - ✅ `baseline_features.py` → `strategies/time_series/features/`
   - ✅ `enhanced_features.py` → `strategies/time_series/features/`
   - ✅ `comprehensive_features.py` → `strategies/time_series/features/`
   - ✅ `labels/` → `strategies/time_series/labels/`
   - ✅ `models/` → `strategies/time_series/models/`
   - ✅ `strategies/*/` → `strategies/time_series/strategies/*/`

3. **导入路径更新**
   - ✅ `comprehensive_features.py` 的导入路径
   - ✅ `sr_reversal/train.py` 的导入路径
   - ⏳ 其他策略的 train.py 文件（待更新）
   - ⏳ 策略的 features.py 文件（待更新）

## ⏳ 待完成

1. **更新所有导入路径**
   - [ ] `sr_breakout/train.py`
   - [ ] `compression_breakout/train.py`
   - [ ] `trend_following/train.py`
   - [ ] 所有策略的 `features.py` 文件

2. **更新 Makefile**
   - [ ] 更新所有 `ts-*` 命令的模块路径

3. **兼容性导入**
   - [ ] 在 `data_tools/` 中添加兼容性导入（deprecated warning）

4. **清理旧代码**
   - [ ] 删除 `strategies/utils/`（如果已整合）
   - [ ] 更新文档

## 📋 新导入路径对照表

### 特征工程

**旧路径：**
```python
from src.data_tools.comprehensive_feature_engineering import ComprehensiveFeatureEngineer
from src.data_tools.baseline_features import BaselineFeatureEngineer
from src.data_tools.feature_engineering_enhanced import EnhancedFeatureEngineer
```

**新路径：**
```python
from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer
from src.strategies.time_series.features.baseline_features import BaselineFeatureEngineer
from src.strategies.time_series.features.enhanced_features import EnhancedFeatureEngineer
```

### 标签

**旧路径：**
```python
from src.strategies.labels.sr_reversal_label import compute_sr_reversal_label
```

**新路径：**
```python
from src.strategies.time_series.labels.sr_reversal_label import compute_sr_reversal_label
```

### 模型训练器

**旧路径：**
```python
from src.strategies.models.strategy_trainer import train_strategy_model
```

**新路径：**
```python
from src.strategies.time_series.models.strategy_trainer import train_strategy_model
```

### 策略训练脚本

**旧路径：**
```python
python -m src.strategies.sr_reversal.train
```

**新路径：**
```python
python -m src.strategies.time_series.strategies.sr_reversal.train
```

## 🔄 兼容性方案

在 `data_tools/` 中添加兼容性导入，保持向后兼容：

```python
# src/data_tools/comprehensive_feature_engineering.py
import warnings
from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer

warnings.warn(
    "Importing ComprehensiveFeatureEngineer from data_tools is deprecated. "
    "Please use: from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer",
    DeprecationWarning,
    stacklevel=2
)
```


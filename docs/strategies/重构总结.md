# 策略模块重构总结

## ✅ 已完成

### 1. 新目录结构创建

```
src/strategies/
├── time_series/              # 时序策略
│   ├── features/             # 时序特征工程（统一管理）
│   │   ├── baseline_features.py
│   │   ├── enhanced_features.py
│   │   └── comprehensive_features.py
│   ├── labels/               # 时序标签
│   │   ├── sr_reversal_label.py
│   │   ├── sr_breakout_label.py
│   │   ├── compression_breakout_label.py
│   │   └── trend_following_label.py
│   ├── models/               # 时序模型训练器
│   │   └── strategy_trainer.py
│   └── strategies/           # 具体策略实现
│       ├── sr_reversal/
│       ├── sr_breakout/
│       ├── compression_breakout/
│       └── trend_following/
└── cross_sectional/          # 横截面策略（预留）
    ├── features/
    ├── labels/
    ├── models/
    └── strategies/
```

### 2. 代码迁移

- ✅ `baseline_features.py` → `strategies/time_series/features/`
- ✅ `enhanced_features.py` → `strategies/time_series/features/`
- ✅ `comprehensive_features.py` → `strategies/time_series/features/`
- ✅ `labels/` → `strategies/time_series/labels/`
- ✅ `models/` → `strategies/time_series/models/`
- ✅ `strategies/*/` → `strategies/time_series/strategies/*/`

### 3. 部分导入路径更新

- ✅ `comprehensive_features.py` 的导入路径
- ⏳ 其他文件的导入路径（待批量更新）

## ⏳ 待完成

### 1. 批量更新导入路径

需要更新以下文件中的导入路径：

**策略训练脚本（4个）：**
- `strategies/time_series/strategies/sr_reversal/train.py`
- `strategies/time_series/strategies/sr_breakout/train.py`
- `strategies/time_series/strategies/compression_breakout/train.py`
- `strategies/time_series/strategies/trend_following/train.py`

**策略特征文件（4个）：**
- `strategies/time_series/strategies/sr_reversal/features.py`
- `strategies/time_series/strategies/sr_breakout/features.py`
- `strategies/time_series/strategies/compression_breakout/features.py`
- `strategies/time_series/strategies/trend_following/features.py`

**导入路径对照：**

```python
# 旧路径 → 新路径

# 特征工程
from src.data_tools.comprehensive_feature_engineering import ComprehensiveFeatureEngineer
→ from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer

# 标签
from src.strategies.labels.sr_reversal_label import compute_sr_reversal_label
→ from src.strategies.time_series.labels.sr_reversal_label import compute_sr_reversal_label

# 模型训练器
from src.strategies.models.strategy_trainer import train_strategy_model
→ from src.strategies.time_series.models.strategy_trainer import train_strategy_model

# 策略特征（移除 utils 依赖，使用 enhanced_features）
from src.strategies.utils.wpt_features import extract_wpt_features
→ # 使用 enhanced_features 中的方法，或创建独立工具函数
```

### 2. 更新 Makefile

更新所有 `ts-*` 命令的模块路径：

```makefile
# 旧路径
python3 -m src.strategies.sr_reversal.train

# 新路径
python3 -m src.strategies.time_series.strategies.sr_reversal.train
```

### 3. 兼容性导入（可选）

在 `data_tools/` 中添加兼容性导入，保持向后兼容：

```python
# src/data_tools/comprehensive_feature_engineering.py
import warnings
from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer

warnings.warn(
    "Importing from data_tools is deprecated. "
    "Please use: from src.strategies.time_series.features.comprehensive_features import ComprehensiveFeatureEngineer",
    DeprecationWarning,
    stacklevel=2
)
```

### 4. 清理旧代码

- [ ] 删除 `strategies/utils/`（如果已整合到 enhanced_features）
- [ ] 更新所有相关文档

## 📋 重构优势

### 1. 职责清晰

- **data_tools**：数据加载、数据预处理、基础工具
- **strategies/time_series/features**：时序特征工程（统一管理）
- **strategies/cross_sectional/features**：横截面特征工程（独立管理）

### 2. 避免重复

- ✅ 时序特征统一在 `time_series/features/` 管理
- ✅ 不再有 `data_tools` 和 `strategies/utils` 的重复实现

### 3. 易于扩展

- ✅ 新增时序策略：在 `strategies/time_series/strategies/` 下添加
- ✅ 新增横截面策略：在 `strategies/cross_sectional/strategies/` 下添加
- ✅ 特征工程独立，策略可复用

### 4. 模块化

- ✅ 特征工程、标签、模型、策略分离
- ✅ 每个模块职责单一，易于测试和维护

## 🚀 下一步行动

1. **批量更新导入路径**（使用 sed 或 Python 脚本）
2. **更新 Makefile** 中的模块路径
3. **测试验证**：运行训练脚本，确保功能正常
4. **更新文档**：同步更新所有相关文档

## 📝 注意事项

1. **WPT/Hilbert/Hurst/Spectrum 工具**：
   - 这些功能已整合到 `enhanced_features.py` 中
   - 如果策略的 `features.py` 需要独立工具函数，可以从 `enhanced_features` 中提取
   - 或者保留 `strategies/utils/` 作为独立工具库（但需要确保不与 `enhanced_features` 重复）

2. **向后兼容**：
   - 建议在 `data_tools` 中添加兼容性导入，给用户时间迁移
   - 在下一个大版本中移除兼容性导入

3. **测试**：
   - 每个阶段完成后运行测试，确保功能正常
   - 特别注意导入路径的正确性


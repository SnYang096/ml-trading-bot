# 快速启动命令.md — A.1 研究 Pipeline + DEPLOY 完整工作流

> 创建时间: 2026-02-23
> 适用范围: 研究 pipeline 端到端验证 + 配置部署到实盘
> 前置条件: BPC/ME/FER/LV 已训练过, data/parquet_data 有数据

---

## ⚡ 使用模式：`--all` vs `--strategy`

> **核心原则**: 日常研究用 `--all`，单策略调试用 `--strategy`

| 模式 | 命令 | 用途 | PCM 联合回测 |
|------|------|------|-------------|
| **正式研究** | `--all` | 全策略串行训练 → PCM 联合回测 → ADOPT 含 PCM 决策 | ✅ 自动运行 (≥2 策略) |
| **调试/单看** | `--strategy fer` | 单策略训练 → 单策略回测 → 只看交易地图 | ❌ 不运行 (无仲裁意义) |

**为什么 PCM 不需要像 Execution 那样有 Tiers?**

- **Execution Tiers** = 同策略内根据 evidence score 选不同止损/止盈 → 策略内部精细化，需要搜索最优参数
- **PCM** = priority + position_scale → 语义决定的 regime 级全局配置，不是 per-trade 优化
- PCM 是 **validate + snapshot**，不是 optimize — 参数在 `constitution.yaml` 和 `pcm_regime.yaml` 中固定

**多策略 ADOPT 决策矩阵** (仅 `--all` 模式生效):

```
单策略 ADOPT  ∧  PCM PASS   →  最终 ADOPT
单策略 ADOPT  ∧  PCM ALERT  →  ADOPT + WARNING
单策略 ADOPT  ∧  PCM ERROR  →  KEEP (需人工检查)
```

**推荐工作流**:
```bash
# 日常使用 (正式) — 全策略训练 + PCM 联合回测 + ADOPT 含 PCM
python scripts/auto_research_pipeline.py --all

# 调试某个策略 (不跑 PCM, 只看单策略交易地图)
python scripts/auto_research_pipeline.py --strategy fer --no-adopt
```

---

## 总览：从研究到实盘的完整路径

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           完整工作流                                     │
│                                                                         │
│  ① 端到端验证 (dry-run)     确认 pipeline 无报错                        │
│  ② 端到端验证 (实际运行)    单策略跑一次完整 pipeline                   │
│  ③ 全策略研究               四策略批量训练 + PCM 联合回测               │
│  ④ 实验管理                 --list / --adopt / --diff                   │
│  ⑤ DEPLOY                   config/ → live/ 自动对比+部署               │
│  ⑥ 监控                     特征漂移 / 周检 / 月检                      │
│                                                                         │
│  研究 → PCM验证 → ADOPT → config/strategies/ → DEPLOY → live/         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 一、端到端验证

> 目的: 跑一次完整 pipeline 确认无报错

### 1.1 Dry-run (只打印命令，不执行，30 秒)

```bash
cd /home/yin/trading/ml_trading_bot

# 单策略 dry-run
python scripts/auto_research_pipeline.py --strategy fer --dry-run  --end-date 2025-11-01
python scripts/auto_research_pipeline.py --strategy bpc --dry-run
python scripts/auto_research_pipeline.py --strategy me --dry-run

# 全部策略 dry-run
python scripts/auto_research_pipeline.py --all --dry-run
```

**预期输出**: 打印 Step 0 ~ Step 10 的命令，无 ERROR。

### 1.2 单策略实际运行 (建议先跑 FER，最快)

```bash
# FER 端到端 (不自动采纳, 纯验证)
python scripts/auto_research_pipeline.py --strategy fer --no-adopt --end-date 2025-11-01
python scripts/auto_research_pipeline.py --strategy fer --no-adopt --end-date 2026-2-01

```

**预期输出**:
- Step 0 ~ 10 全部通过 (或 Step 0 跳过 —— 数据已有)
- 最终显示 ADOPT/KEEP/ALERT 决策
- 实验保存到 `results/research_history/fer/{timestamp}/`

**验证点**:
```bash
# 检查产出
ls results/research_history/fer/       # 应有 YYYYMMDD_HHMMSS 目录
ls results/research_history/fer/*/     # 应有 report.json, strategies/fer/archetypes/, pipeline.log
cat results/research_history/fer/*/report.json | python -m json.tool | head -20
```

### 1.3 全策略端到端验证

```bash
# 全部策略 (不自动采纳)
python scripts/auto_research_pipeline.py --all --no-adopt
```

**耗时预估**: BPC ~2h, ME ~1.5h, FER ~1.5h (取决于 Feature Store 是否已建)

---

## 二、研究 Pipeline 日常使用

### 2.1 一键研究 (推荐)

```bash
cd /home/yin/trading/ml_trading_bot

# 单策略 (自动检测最新数据日期, ADOPT 时自动覆盖 config/)
python scripts/auto_research_pipeline.py --strategy fer
python scripts/auto_research_pipeline.py --strategy bpc
python scripts/auto_research_pipeline.py --strategy me

# 全部策略
python scripts/auto_research_pipeline.py --all

# 指定数据截止日期
python scripts/auto_research_pipeline.py --strategy fer --end-date 2026-02-01
```

### 2.2 只保存实验，不自动采纳

```bash
# ADOPT 决策时不覆盖生产 config, 需后续手动 --adopt
python scripts/auto_research_pipeline.py --strategy fer --no-adopt
```

---

## 重要说明：数据源与常见陷阱

### 陷阱 1: predictions.parquet vs logs_gated.parquet

> **关键提醒**: Execution 优化和回测务必使用 `logs_gated.parquet`，不要用 `predictions.parquet`。
>
> 两个文件**行数相同**（如 FER 都是 3005 行），区别在于：
>
> | 文件 | 产出步骤 | gate 标记列 | 脚本行为 |
> |------|---------|-----------|----------|
> | `predictions.parquet` | Step 6 Evidence apply | **无** | 全部信号参与 → 虚高胜率(96%) |
> | `logs_gated.parquet` | Step 5 Gate apply | **有** `gate_decision` | 自动过滤 → 只有 allow 信号参与 |
>
> **为什么重要**:
> 1. `optimize_execution_grid.py` 会自动检测 `gate_decision` 列并过滤 veto 信号
> 2. `predictions.parquet` 没有该列 → 脚本无法过滤 → 全部信号参与 → 虚高胜率(96.1%)
> 3. 使用错误数据源会产生不合理的参数（如 initial_r=6.0）
> 4. 正确做法：用 `logs_gated.parquet`，回测结果(如54.2%胜率)才与实盘一致

### 陷阱 2: Gate 通过率低导致样本不足

> Gate 过滤后可用信号量可能很少（如 FER: 120/3005=4%），导致 execution 优化只有个位数交易，
> 结果不可靠（如 7 trades + 100% win rate + Sharpe 21）。
>
> **应对**:
> - `auto_research_pipeline` 已内置小样本保护：`gate allows < min_trades` 时自动跳过 Step 8，保留现有 execution.yaml
> - 手动运行时需自行判断交易数是否足够（建议 ≥30 trades）

### 陷阱 3: 目录名 ≠ 内容范围

> `train_final_*_return_tree/` 中的 `return_tree` 仅标识 Step 2 prepare 使用的 label 类型，
> 但 **Step 3–10 全流程产出都写入同一目录**，包括 gate、evidence、execution 等阶段的文件。
> 不要因目录名误以为里面只有 return_tree 相关内容。

---

## 三、实验管理

> **新增功能**: 批量删除历史实验 (`cleanup_old_experiments.py`)
> - 根据时间戳、状态、日期范围批量删除实验
> - 支持dry-run模式预览删除内容
> - 释放磁盘空间，保持实验历史整洁

### 3.1 列出历史实验

```bash
# 单策略
python scripts/auto_research_pipeline.py --strategy bpc --list

# 全部策略
python scripts/auto_research_pipeline.py --all --list
```

**输出说明**:
- 时间戳: 实验创建时间 (YYYYMMDD_HHMMSS)
- 创建时间: 实际创建时刻 (YYYY-MM-DD HH:MM)
- Sharpe: 年化夏普比率
- Trades: 交易次数
- 决策: ADOPT/KEEP/ALERT/ERROR
- 备注: 数据范围等信息

### 3.2 手动采纳实验

```bash
# 采纳指定实验 → 覆盖 config/strategies/fer/archetypes/
python scripts/auto_research_pipeline.py --strategy fer --adopt 20260222_120000
```

### 3.3 对比两次实验 (漂移报告)

TS = 实验目录的时间戳名称 (如 `20260223_121438`) ，可通过 `--list` 查看。

```bash
# 先列出可用的实验时间戳
python scripts/auto_research_pipeline.py --strategy fer --list

# 输出示例:
#   20260223_114526       0.3797        440    ✅ ADOPT  2023-01-01~2026-02-01 (训练耗时: 1h23m)
#   20260223_121438       0.5064        449    ✅ ADOPT  2023-01-01~2026-02-01 (训练耗时: 1h15m)
#   20260223_121948       0.5059        440    ✅ ADOPT  2023-01-01~2026-02-01 (训练耗时: 1h28m)
#
# 实际输出格式:
#   时间戳                        创建时间            Sharpe   Trades       决策  备注
#   20260224_122314            2026-02-24 12:25        0.5310     1656 ✅ ADOPT  2023-01-01~2026-02-01

# 对比任意两次实验 → 输出结构化漂移报告
python scripts/auto_research_pipeline.py --strategy fer --diff 20260223_121438 20260223_121948

python scripts/auto_research_pipeline.py --strategy fer --diff 20260223_114526 20260223_121438

```

**漂移报告输出**:
- Metrics 对比表 (Sharpe / Trades / WinRate / MeanR 变化百分比)
- 逐文件分析: prefilter 阈值漂移、gate 规则增删、execution 参数变化等
- 每个文件的漂移等级: 🟢 LOW / 🟡 MEDIUM / 🔴 HIGH
- 综合判定 + 建议: STABLE / MONITOR / REVIEW / ADJUST

> **注意**: 每次 pipeline 跑完时，如果存在上一次实验，会**自动输出漂移报告**，无需手动 `--diff`。

### 3.4 批量删除历史实验

> 目的: 根据时间戳批量删除无用的历史实验，释放磁盘空间

```bash
# 删除指定时间戳的单个实验
python scripts/cleanup_old_experiments.py --strategy fer --timestamp 20260223_121948

# 批量删除多个时间戳的实验
python scripts/cleanup_old_experiments.py --strategy fer --timestamp 20260223_114526 20260223_121948 20260223_121438

# 删除指定策略的全部历史实验（谨慎使用）
python scripts/cleanup_old_experiments.py --strategy fer --all 

# 删除指定策略的错误实验（ERROR状态）
python scripts/cleanup_old_experiments.py --strategy fer --status error

# 示例：删除ME策略的错误实验
python scripts/cleanup_old_experiments.py --strategy me --status error

python scripts/cleanup_old_experiments.py --strategy bpc --status error


# 删除指定日期范围内的实验
python scripts/cleanup_old_experiments.py --strategy fer --date-range "2026-02-20" "2026-02-22"

# dry-run 模式（只显示将要删除的内容，不实际删除）
python scripts/cleanup_old_experiments.py --strategy fer --timestamp 20260223_121948 --dry-run
```

**注意事项**:
- 仅删除 `results/research_history/{strategy}/{timestamp}/` 目录
- 不影响 `config/strategies/` 和 `live/` 中的生产配置
- 建议先使用 `--dry-run` 确认将要删除的内容
- 删除后无法恢复，请谨慎操作

---

## 四、DEPLOY (config → live)

> 将研究确认的 config/strategies/ 部署到 live/highcap/config/strategies/
> 脚本: `scripts/deploy_config_to_live.py`

### 4.0 Deploy 触发条件

**不是每次研究变了就 deploy。** 核心逻辑：“旧的不行了，新的更好” 才 deploy。

规则配置在 `config/research_pipeline.yaml` 的 `deploy_gate:` 中：

**触发条件 (OR, 至少一个满足才值得 deploy):**

| 规则 | 条件 | 含义 |
|------|------|------|
| sharpe_improve | 提升 >= 5% | 新版本显著更好 |
| drift_level | >= HIGH 且 \|Sharpe变化\| > 3% | 参数大幅漂移 + 性能确实有变 |

> **稳定性保护**: 即使漂移达 HIGH，若 Sharpe 变化 <= 3%，则不触发——防止数据窗口滑动导致的自然漂移误触发。

**安全门禁 (AND, 全部满足才允许 deploy):**

| 规则 | 条件 | 含义 |
|------|------|------|
| require_adopt | 决策 = ADOPT | 新版不比旧版差 |
| min_trades | >= 50 | 统计显著性保障 |
| human_confirm | 默认 true | 部署前需人工确认 |

**决策矩阵:**

| 漂移\\Sharpe变化 | 提升>=5% | \|变化\|>3% | \|变化\|<=3% |
|---|---|---|---|
| HIGH | ✅ DEPLOY | ✅ DEPLOY | ❌ SKIP (漂移大但性能没变) |
| < HIGH | ✅ DEPLOY | ❌ SKIP | ❌ SKIP |

pipeline 跑完会自动显示：
- `🚀 Deploy: ✅ 值得且允许 deploy` + deploy 命令
- `⏭️  Deploy: SKIP` — Sharpe 提升不足 且 漂移较小/性能稳定, 不需要动实盘
- `🚫 Deploy: ❌` — 有触发但安全门禁未通过 + 具体原因

结果保存在 `report.json` 的 `deploy_gate` 字段中。

### 4.1 查看差异 (不部署)

```bash
# 查看所有策略的 config/ vs live/ 差异
python scripts/deploy_config_to_live.py --diff

# 只看某个策略
python scripts/deploy_config_to_live.py --diff --strategy bpc
```

### 4.2 执行部署

```bash
# 部署所有策略 (交互确认)
python scripts/deploy_config_to_live.py --deploy

# 部署指定策略
python scripts/deploy_config_to_live.py --deploy --strategy bpc me

# 部署 + 自动 git commit
python scripts/deploy_config_to_live.py --deploy --git-commit

# 非交互模式 (自动化/CI)
python scripts/deploy_config_to_live.py --deploy --yes --git-commit
```

### 4.3 回滚

```bash
# 查看回滚指引 + 最近的部署 commit
python scripts/deploy_config_to_live.py --rollback

# 手动回滚上一次部署
git revert HEAD

# 手动恢复到指定版本
git checkout <commit-hash> -- live/highcap/config/strategies/
```

### 4.4 完整部署流程 (研究 → 实盘)

```bash
# Step 1: 运行研究 pipeline
python scripts/auto_research_pipeline.py --strategy fer

# Step 2: 确认决策 (应显示 ADOPT)
python scripts/auto_research_pipeline.py --strategy fer --list

# Step 3: 查看 config/ vs live/ 差异
python scripts/deploy_config_to_live.py --diff --strategy fer

# Step 4: 部署到 live/
python scripts/deploy_config_to_live.py --deploy --strategy fer --git-commit

# Step 5: 验证部署
ls live/highcap/config/strategies/fer/archetypes/
```

---

## 五、一键配置健康检查 (推荐)

> 脚本: `scripts/check_need_retrain.py`
> 替代 5 个独立监控命令, 自动发现数据 + 趋势分析 + 给出结论

### 5.0 快速开始

```bash
# 单策略 (自动发现最近训练数据, 不足时自动补训)
python scripts/check_need_retrain.py --strategy fer

# 全策略
python scripts/check_need_retrain.py --all

# 只看已有数据, 不自动补训
python scripts/check_need_retrain.py --strategy fer --no-fill

# 指定最少月数 (默认 6)
python scripts/check_need_retrain.py --strategy fer --min-months 8

# 补训时 dry-run (只打印命令)
python scripts/check_need_retrain.py --all --dry-run
```

**输出**: Sharpe/Trades/漂移趋势表 + 明确结论 (STABLE/ATTENTION/RETRAIN)

**典型工作流**:
```bash
# 1. 一键检查 → 看到 RETRAIN
python scripts/check_need_retrain.py --all --no-fill

# 2. 重训
python scripts/auto_research_pipeline.py --strategy fer

# 3. 部署
python scripts/deploy_config_to_live.py --deploy --strategy fer --git-commit
```

### 5.1 底层监控命令 (通常不需要直接用)

<details>
<summary>展开查看底层命令</summary>

#### 特征漂移检测
```bash
python scripts/local_monitor_feature_drift.py \
  --baseline results/train_final_20260223_114606_return_tree/fer/training_baseline.json \
  --new-data results/train_final_20260223_161624_return_tree/fer/predictions.parquet
```

#### 周频快速检查
```bash
python scripts/local_monitor_weekly.py \
  --baseline results/train_final_20260223_114606_return_tree/fer/training_baseline.json \
  --data results/train_final_20260223_161624_return_tree/fer/predictions.parquet \
  --strategy fer
```

#### 月频全层报告
```bash
python scripts/local_monitor_monthly.py \
  --baseline results/train_final_20260223_114606_return_tree/fer/training_baseline.json \
  --data results/train_final_20260223_161624_return_tree/fer/predictions.parquet \
  --strategy fer
```

#### 重训触发检查
```bash
python scripts/monitor_retrain.py --check-only
```

</details>

---

## 六、手动分步执行 (调试用)

适用于某一步失败需要单独重跑的场景。

```bash
STRATEGY="fer"              # bpc / fer / me
TIMEFRAME="240T"            # fer/bpc=240T, me=60T
SYMBOLS="BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT,ADAUSDT"
START="2023-01-01"
END="2026-03-01"
HOLDOUT="2025-01-01"
```

| Step | 命令 | 说明 |
|------|------|------|
| 0 | `bash scripts/download_all_data.sh` | 数据下载 (OI + Funding Rate 历史) |
| 0.5 | `python scripts/refresh_funding_oi_data.py --lookback-days 30` | 增量刷新 OI + Funding Rate (最近30天) |
| 1 | `mlbot feature-store build --no-docker --config config/strategies/${STRATEGY} --symbols ${SYMBOLS} --timeframe ${TIMEFRAME} --start-date ${START} --end-date ${END} --warmup-months 6` | Feature Store |
| 2 | `mlbot train final --no-docker --prepare-only --config config/strategies/${STRATEGY} --features config/strategies/${STRATEGY}/features_gate.yaml --labels config/strategies/${STRATEGY}/labels_rr_extreme.yaml --symbol ${SYMBOLS} --timeframe ${TIMEFRAME} --data-path data/parquet_data --start-date ${START} --end-date ${END} --holdout-start-date ${HOLDOUT} --holdout-end-date ${END} --seed 42` | Prepare |
| 3 | `python scripts/analyze_archetype_feature_stratification.py --logs <PREPARE_DIR>/features_labeled.parquet --strategy ${STRATEGY} --select-recent 6 --promote` | Prefilter |
| 4 | `python z实验_005_统一研究/direction_strict_validation.py --logs <PREPARE_DIR>/features_labeled.parquet --strategy ${STRATEGY} --compare-features --temporal --promote` | Direction |
| 7 | `python scripts/optimize_entry_filter_plateau.py --logs <EV_DIR>/logs_gated.parquet --strategy ${STRATEGY} --research --promote` | Entry Filter |
| 8 | `python scripts/optimize_execution_grid.py --logs <EV_DIR>/logs_gated.parquet --strategy ${STRATEGY} --output <EV_DIR>/execution_grid.json --promote` | Execution |
| 9 | `python scripts/backtest_execution_layer.py --logs <EV_DIR>/logs_gated.parquet --strategy ${STRATEGY}` | Backtest |

> 完整手动步骤参见: `z实验_006_统一实盘/本地研究pipeline命令.md` 第四节

---

## 七、关键路径总览

```
config/research_pipeline.yaml          ← 统一配置 (时间窗口/策略/决策规则/deploy门禁)
scripts/check_need_retrain.py           ← 一键配置健康检查 (自动发现+趋势+结论, 推荐入口)
scripts/auto_research_pipeline.py      ← 一键研究 (11步训练链)
scripts/deploy_config_to_live.py       ← 部署到实盘 (diff + copy + git)
scripts/test_rolling_deploy_gate.py    ← 滚动 deploy 门禁模拟 (N月回测)
scripts/monitor_retrain.py             ← 重训触发器 (cron 用)

config/strategies/{bpc,me,fer}/        ← 研究确认的生产 config (git)
live/highcap/config/strategies/        ← 实盘读取的 config (git)
results/research_history/              ← 实验历史 (不进 git)
results/rolling_deploy_test/           ← 滚动测试结果 (不进 git)
reports/health_check/                  ← 健康检查报告 (不进 git)
```

**决策链路**:

```
研究实验 (results/research_history/)
  → ADOPT → config/strategies/ (研究确认, git)
  → DEPLOY → live/highcap/config/strategies/ (生产部署, git)
```

**决策规则 (全自动, 无人工)**:

| 条件 | 决策 |
|------|------|
| trades < 10 | ERROR |
| sharpe <= 0 | ALERT |
| new/prev >= 0.7 | ADOPT → 自动覆盖 config/ |
| new/prev < 0.7 | ALERT (显著衰减) |
| 首次运行 | ADOPT |

---

## 八、滚动 Deploy 门禁模拟

> 用途: 模拟最近 N 个月, 逐月跑完整 pipeline, 检查哪些月份将触发 deploy
> 脚本: `scripts/test_rolling_deploy_gate.py`

### 8.1 单策略

```bash
# FER 最近 10 个月
python scripts/test_rolling_deploy_gate.py --strategy fer

# BPC 最近 6 个月
python scripts/test_rolling_deploy_gate.py --strategy bpc --months 6

# 断点续跑 (跳过已跑的月份)
python scripts/test_rolling_deploy_gate.py --strategy fer --resume
```

### 8.2 多策略 / 全策略

```bash
# 指定多个策略
python scripts/test_rolling_deploy_gate.py --strategy fer bpc

# 全部策略 (自动发现 config 中的 bpc/fer/me)
python scripts/test_rolling_deploy_gate.py --all

# 全策略 dry-run
python scripts/test_rolling_deploy_gate.py --all --dry-run
```

### 8.3 输出说明

- 每个策略输出独立的汇总表 (月份 / Sharpe / 漂移 / Deploy 状态)
- 多策略时额外输出交叉汇总 (各策略 Deploy率对比)
- 结果保存到 `results/rolling_deploy_test/{strategy}/rolling_summary.json`

### 8.4 典型工作流

```bash
# 1. 先调整 deploy 门禁参数
vim config/research_pipeline.yaml  # 修改 deploy_gate 节

# 2. 用滚动测试验证门禁灵敏度
python scripts/test_rolling_deploy_gate.py --strategy fer --months 10

# 3. 查看 Deploy率 — 目标: 10%~30% (不是每月都 deploy, 也不是永远不 deploy)
```

---

## 九、事件驱动回测 (Event-Driven Backtest)

>目的: 用真实1分钟tick逐个触发信号，完全模拟实盘时序行为，与向量化回测对比验证
>核心: 1min bar 模拟器 + LivePCM +共持仓管理逻辑
>: `scripts/event_backtest.py`

### 9.1基本用法

```bash
# ── 默认: 使用研究数据 (data/parquet_data)，无需任何额外参数 ──
python scripts/event_backtest.py --strategy bpc,fer,me

# 指定天数
python scripts/event_backtest.py --strategy bpc,fer,me --days 365

# 单策略 + 指定 symbol
python scripts/event_backtest.py --strategy fer --days 90 --symbols BTCUSDT,ETHUSDT

# ── 可选: 用实盘数据验证 (live 目录仅用于实盘, 此处仅作验证) ──
python scripts/event_backtest.py --strategy bpc,fer,me --data-path none --live-root live/highcap

# 导出交易明细 CSV
python scripts/event_backtest.py --strategy bpc --days 90 --export results/event_backtest/bpc_trades.csv

# 保存 JSON 结果
python scripts/event_backtest.py --strategy bpc --days 90 --output results/event_backtest/bpc_result.json
```

> **数据源选择** (事件回测 + 向量回测通用):
> | 参数 | 数据源 | 加载方式 | 适用场景 |
> |------|--------|---------|----------|
> | `--data-path data/parquet_data` (默认) | 研究 tick 数据 | DataHandler → 1min resample | 所有回测 (推荐, 无需额外参数) |
> | `--data-path none --live-root live/highcap` | 实盘 1min bars | StorageManager → bar_1min | 仅用于验证实盘数据正确性 |
>
> **原则**: `live/` 目录是实盘专属，回测默认不触碰。配置也统一用 `config/strategies`，不依赖 `live/highcap/config`。

### 9.1b 订单落库 + 交易地图 (NEW)

> 集成 order_management mock 开仓，每笔交易写入 SQLite，并生成 4H K线交易地图

```bash
# 订单落库到 SQLite (mock 开仓/平仓，保存 orders + positions 表)
python scripts/event_backtest.py --strategy bpc --days 90 \
  --db results/event_backtest/backtest_bpc.db

# 订单落库 + 交易地图 (4H K线 + 入场/出场标记, Bokeh HTML)
python scripts/event_backtest.py --strategy bpc --days 90 \
  --db results/event_backtest/backtest_bpc.db \
  --trading-map results/event_backtest/trading_map_bpc.html

# 只要交易地图，不落库
python scripts/event_backtest.py --strategy fer --days 60 \
  --trading-map results/event_backtest/trading_map_fer.html

# 全参数示例
python scripts/event_backtest.py --strategy bpc \
  --symbols BTCUSDT,ETHUSDT,SOLUSDT \
  --days 180 \
  --db results/event_backtest/backtest_bpc.db \
  --trading-map results/event_backtest/trading_map_bpc.html \
  --export results/event_backtest/bpc_trades.csv \
  --output results/event_backtest/bpc_result.json
```

**交易地图说明**:
- 每个 symbol 独立一个 4H K线面板
- 入场标记: ▲ (LONG) / ▽ (SHORT), 绿色=盈利, 红色=亏损
- 出场标记: ■, 虚线连接入场→出场
- 支持缩放/平移/悬浮提示
- 依赖: `pip install bokeh` (未安装时自动跳过)

**订单落库说明**:
- 使用 `MockBinanceAPI` 模拟即时成交，无网络调用
- 数据写入 SQLite: `positions` 表 (开仓/平仓) + `orders` 表 (买卖订单)
- 可用 `sqlite3 results/event_backtest/backtest_bpc.db "SELECT * FROM positions"` 查询
- 依赖: `src/order_management` 模块 (已内置)

### 9.2 与研究回测对比

```bash
# 1.先跑事件回测 (多策略联合, 默认用研究数据)
python scripts/event_backtest.py --strategy bpc,fer,me --days 365

# 2. 查看研究回测结果 (ADOPT后自动保存)
cat results/research_history/bpc/20260226_*/report.json | python -m json.tool | grep -E "sharpe_per_trade|total_trades"
```

**多策略事件回测基准 (2026-03-01, 1min grid search 优化参数)**:
```
交易数:  395     胜率: 84.6%    Sharpe(R): 0.2128    Mean R: 0.1436

信号漏斗 (修复后 4 桶分类):
  total_bars:             25965
  reject_no_direction:        0
  reject_gate_deny:       24481
  reject_entry_filter:        0
  reject_pcm_slot_full:    1484 (pre-slot 信号 1879)
  signals_generated:        395
```

**向量回测基准 (--from-raw --use-1min, 同期)**:
```
交易数: 1332     胜率: 79.3%    Sharpe(R): 0.1862
pre-slot 信号: 1895 (与事件侧 1879 已对齐)
slot 拒绝率: 29.7% (事件侧 79.1%)
```

> **向量 vs 事件回测差异根因 (2026-03-01 诊断结果)**:
> - **pre-slot 信号已对齐**: 1895 (向量) ≈ 1879 (事件)，gate/direction/entry_filter 层一致
> - **slot 行为是唯一差距**: 向量用 bar 级 exit_idx 判定 slot 释放（4H 粒度），事件用 1min 实时持仓追踪
> - **已修复**: 向量回测 slot 判定改为 1min exit_ts_ns 时间戳精度，待验证收敛
> - 事件回测更接近真实实盘行为，以此为准

### 9.3 关键差异分析

|差异点 | 事件回测 |研回测 |影响 |
|--------|----------|----------|------|
| **信号触发** | 1min tick 逐个处理 |向批量扫描 | 时序更真实 |
| **持仓管理** | 实盘 same logic |回简化逻辑 |出场更保守 |
| **SL/TP** | high/low/close 分开 |同一价格 |止优先 |
| **PCM slot** | 实时全局控制 | 无slot限制 | 交易数更少 |
| **特征计算** |实时 |全预计算 | 一致 |

### 9.4 诊断工具

```bash
# 查看信号漏斗 (total_signals → gate_deny → generated → max_positions → trades)
python scripts/event_backtest.py --strategy bpc --days 30 --symbol BTCUSDT | grep -A 10 "信号漏斗"

# 查看持仓明细
python scripts/event_backtest.py --strategy fer --days 10 --symbol BTCUSDT --verbose

#性能分析 (profiling)
python -m cProfile -o evt_bt.prof scripts/event_backtest.py --strategy bpc --days 7 --symbol BTCUSDT
```

### 9.5 最佳实践

1. **日常验证**: `--all`后，用 30-90天事件回测验证关键策略
2. **差异诊断**: Sharpe差 > 30% 时深入分析信号漏斗和持仓行为
3. **实盘对齐**: 事件回测是实盘前的最后一道验证关卡

### 9.6b Evidence 诊断工具

> 脚本: `scripts/diagnose_evidence_roi.py`
> 用途: 验证 evidence score 预测力 + 量化 slot 竞争 ROI

```bash
# 三策略合并诊断 (推荐)
python scripts/diagnose_evidence_roi.py \
  --logs bpc:results/train_final_20260228_154956_rr_extreme/bpc/logs_gated.parquet \
        fer:results/train_final_20260228_155621_rr_extreme/fer/logs_gated.parquet \
        me:results/train_final_20260228_160522_rr_extreme/me/logs_gated.parquet

# 单策略
python scripts/diagnose_evidence_roi.py \
  --logs results/.../bpc/logs_gated.parquet --strategy bpc
```

**诊断输出**:
- Q1: Evidence 预测力 — 按四分位分层，Mean R / Win% / Sharpe 是否单调递增
- Q2: Slot 竞争模拟 — FCFS (先到先得) vs Evidence Ranking 对比 Sharpe
- Q3: Per-Strategy 分析 — 每策略 evidence 方向正确性

**历史诊断结果 (2026-02-28)**:
```
策略    Evidence Corr    预测力
BPC     +0.104          ✅ 正向
FER     +0.127          ✅ 正向 (修复后, 原为 -0.127)
ME      +0.022          ⚠️  弱正
```

> **重要修复**: FER evidence 三个特征的 labels 顺序被翻转 (suppress→amplify 改为 amplify→suppress)，
> 修复前 evidence score 与实际表现**反向**，导致 slot 竞争时系统性选差信号。
> 配置: `config/strategies/fer/archetypes/evidence.yaml`

### 9.6 特征一致性验证 (研究路径 vs 实盘路径)

> 目的: 验证研究 DataHandler(4H resample) 与实盘 IncrementalFeatureComputer(1min→4H) 对相同数据计算出的特征值一致
> 脚本: `scripts/compare_same_data.py`

```bash
# 基本用法 (默认 BPC 策略, BTCUSDT, 最近1年)
python scripts/compare_same_data.py

# 指定策略和时间范围
python scripts/compare_same_data.py --strategy fer --days 180
```

**预期输出**:
```
特征匹配分布:
  完美 (>=99.9%):  36/36
  良好 (90-99.9%): 0/36
  较差 (<90%):     0/36

所有信号完全一致!
```

**关键检查项**:
| 检查项 | 健康标准 | 说明 |
|--------|---------|------|
| 方向一致 | 100% | 研究 vs 实盘方向判定完全一致 |
| Gate 一致 | 100% | Gate 过滤结果一致 |
| Entry Filter 一致 | 100% | 入场过滤一致 |
| 最终信号一致 | 100% | 所有信号链路端到端一致 |
| 特征匹配 (>=99.9%) | 36/36 | 所有特征数值一致 |

> **历史 Bug**: CVD 相关特征 (`shd_pct`, `bpc_cvd_z`) 曾因 `data_handler.py` 中
> `_resample_from_ohlc()` 对 CVD 累积列错误使用 `"sum"` 聚合 + 月边界 cumsum 断裂，
> 导致匹配率仅 2.2% / 89.1%。已通过 `_recompute_cvd_columns()` 修复 (在跨月 concat 后
> 从 buy_qty/sell_qty 重新计算所有 CVD 列)。
>
> 修复后需删除旧 cache: `rm cache/timeframes/*.parquet`

---

## 十、PCM 联合回测 + KPI 评估

> 目的: 多策略联合仲裁回测 + Regime-Aware 仓位缩放验证 + KPI 门禁评估
> 前置条件: 至少 2 个策略已完成 Step 5+ 训练，有 logs_gated.parquet（经过gate过滤的有效信号）
> 配置文件: `config/pcm_regime.yaml` (仲裁策略), `config/constitution/constitution.yaml` (硬约束)
>
> **Evidence Slot 竞争机制 (2026-02-28 新增)**:
> slot 满时不再 "先到先得" 直接拒绝，而是按 evidence score 竞争排序：
> - 新信号 evidence > 最弱 active slot evidence → **驱逐最弱，接纳新信号**
> - 新信号 evidence ≤ 最弱 → 拒绝（与之前行为一致）
> - 实现: `live_pcm.py._try_slot_competition()` (实盘/事件回测) + `simulate_rr_execution()` 后处理 (向量回测)

### 9.1 PCM 联合回测 (手动)

```bash
cd /home/yin/trading/ml_trading_bot

# ── 变量设置 ──
BPC_DIR="results/train_final_<timestamp>_rr_extreme/bpc"
FER_DIR="results/train_final_<timestamp>_rr_extreme/fer"
ME_DIR="results/train_final_<timestamp>_rr_extreme/me"

# ── 双策略联合回测 (BPC + FER) ──
# 注意：使用logs_gated.parquet确保与实盘一致（经过gate过滤）
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet

# ── 三策略联合回测 (BPC + FER + ME) ──
# 注意：使用logs_gated.parquet确保与实盘一致（经过gate过滤）
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet \
       me:${ME_DIR}/logs_gated.parquet

# ── 1min bar 精度 + slot 限制 (推荐, 默认用研究数据) ──
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet \
       me:${ME_DIR}/logs_gated.parquet \
  --use-1min --max-slots 3

# ── --from-raw 模式: 从原始数据重算特征+gate, 不依赖 logs_gated (一致性验证用) ──
python scripts/backtest_execution_layer.py \
  --pcm bpc fer me --from-raw \
  --test-start 2025-08-01 --test-end 2026-01-31 \
  --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,XRPUSDT,DOGEUSDT \
  --use-1min --recompute-gate

# ── 可选: 用实盘数据验证 (live 目录仅用于实盘, 此处仅作验证) ──
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet \
       me:${ME_DIR}/logs_gated.parquet \
  --use-1min --max-slots 3 --data-path none --live-root live/highcap

# ── 带 Tiers + Evidence Quantile 校准 ──
# 注意：使用logs_gated.parquet确保与实盘一致（经过gate过滤）
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet \
  --quantile-train-start 2025-02-01 \
  --quantile-train-end 2025-08-01

# ── 输出交易地图到指定文件 ──
# 注意：使用logs_gated.parquet确保与实盘一致（经过gate过滤）
python scripts/backtest_execution_layer.py \
  --pcm bpc:${BPC_DIR}/logs_gated.parquet \
       fer:${FER_DIR}/logs_gated.parquet \
  --output results/pcm_trading_map.html

# 注意：使用logs_gated.parquet确保与实盘一致（经过gate过滤）
cd /home/yin/trading/ml_trading_bot && python scripts/backtest_execution_layer.py \
  --pcm bpc:results/train_final_20260226_225235_rr_extreme/bpc/logs_gated.parquet \
       fer:results/train_final_20260226_225635_rr_extreme/fer/logs_gated.parquet \
       me:results/train_final_20260226_220758_rr_extreme/me/logs_gated.parquet \
  --strategies-root config/strategies \
  --quantile-train-start 2025-02-01 --quantile-train-end 2025-08-01 \
  > /tmp/pcm_vector_q.log 2>&1 &
echo "PID=$!"
```

**回测输出**:
- 总体 Sharpe (per-trade / annualized / daily)
- Per-Archetype 统计: Trades / Mean R / Sharpe / Win%
- Regime 分布: NORMAL / HIGH_VOL / HIGH_LEVERAGE 各占比 + 入场数
- 冲突率: 多策略同时触发信号的比例
- 反事实分析: 被 PCM 丢弃信号的后续 R-multiple

### 9.2 关键指标解读

| 指标 | 健康范围 | 说明 |
|------|---------|------|
| Sharpe (daily) | > 1.0 | 业界可比指标 |
| 冲突率 | < 15% | 策略互补性好 |
| Regime 分布 | NORMAL > 60% | 非极端市场占比 |
| HIGH_VOL 缩放 | 0.5x | 高波动全局减仓 50% |
| 反事实 Mean R | < 主策略 | 被丢弃的信号应较差 |

### 9.3 PCM 配置验证

```bash
# 验证 constitution.yaml 与 pcm_regime.yaml 一致性
python -c "
import yaml
const = yaml.safe_load(open('config/constitution/constitution.yaml'))
pcm = yaml.safe_load(open('config/pcm_regime.yaml'))

# 1. pcm_regime 不应有 max_slots
assert 'max_slots' not in pcm, 'pcm_regime.yaml 不应包含 max_slots'

# 2. constitution_ref 指向正确
assert pcm.get('constitution_ref') == 'config/constitution/constitution.yaml'

# 3. slot_count 存在
assert const['slots']['slot_count'] == 3

# 4. per_strategy_limits 完整
for s in ['bpc', 'me', 'fer', 'lv']:
    assert s in const['resource_allocation'], f'{s} missing'

print('✅ PCM + Constitution 配置一致性验证通过')
"
```

### 9.4 PCM KPI 门禁 (config/kpi_gates/pcm_arbitration.yaml)

回测完成后，可对照 KPI 门禁检查结果:

| 门禁类型 | 指标 | 范围 | 含义 |
|---------|------|------|------|
| **hard_fail** | sharpe_per_trade | [0, 10] | 仲裁后基本盈利能力 |
| **hard_fail** | conflict_rate | [0, 0.30] | 冲突率不能过高 |
| **hard_fail** | win_rate | [0.35, 0.75] | 排除异常极端 |
| **hard_fail** | max_drawdown | [0, 0.25] | 最大回撤 |
| **warn** | regime_scale_drag | [0, 0.20] | Regime缩放拖累不超过20% |
| **warn** | constitution_kill_count | [0, 3] | 熔断触发次数 |
| **warn** | rejected_mean_r | [-∞, +∞] | 被拒信号反事实 |

### 10.5 集成到 auto_research_pipeline (TODO)

> **当前状态**: Step 9.5 PCM 联合回测已集成到自动 pipeline (`--all` 模式自动运行)。
> **设计方案**: 详见 `z实验_005_统一研究/PCM宪法统一架构完整设计文档.md` §4

**目标**: `auto_research_pipeline.py` Step 9 完成后，自动扫描其他策略的 predictions，
若找到 ≥1 个其他策略，自动跑 PCM 联合回测，输出 `pcm_stats.json`，纳入决策：

```
Step 9:    Single-Strategy Backtest
Step 9.5:  PCM Joint Backtest (NEW)      ← 自动收集 + 联合回测 + pcm_stats.json
Step 10:   Export Training Baseline
  ↓
单策略决策 = ADOPT  AND  PCM决策 = PASS   → 最终 ADOPT
单策略决策 = ADOPT  AND  PCM决策 = ALERT  → 最终 ADOPT + WARNING
单策略决策 = ADOPT  AND  PCM决策 = ERROR  → 最终 KEEP (需人工检查)
```

**实现 TODO** (Phase 6):
- [ ] `auto_research_pipeline.py` 新增 Step 9.5: 扫描 `results/` 最新 predictions + 调用 `--pcm`
- [ ] 新增 `--pcm-stats-json` 参数输出结构化 JSON
- [ ] 扩展 `save_report()`: 包含 pcm_stats
- [ ] 扩展对比决策: conflict_rate / constitution_sim / pcm_sharpe

---

## 十、常见问题

### Q: 研究数据 (Funding Rate / OI) 过期了怎么办?

A: 用增量刷新脚本更新到最新:
```bash
# 刷新最近 30 天 (Binance 公开 API，不需要 API key)
python scripts/refresh_funding_oi_data.py --symbols ADAUSDT,BNBUSDT,BTCUSDT,ETHUSDT,SOLUSDT,XRPUSDT --lookback-days 30

# 刷新最近 60 天 (脚本自动分段查询，OI API 限制 30 天/段)
python scripts/refresh_funding_oi_data.py --symbols ADAUSDT,BNBUSDT,BTCUSDT,ETHUSDT,SOLUSDT,XRPUSDT --lookback-days 60
```
输出: `data/funding_rate/parquet/` 和 `data/open_interest/parquet/`，与研究 pipeline 完全兼容。

> 历史全量数据用 `bash scripts/download_all_data.sh` (通过 Binance Data Vision，数据延迟 ~1 个月)。
> 增量刷新用 `refresh_funding_oi_data.py` (通过 Binance REST API，实时数据)。

### Q: Feature Store 太慢怎么办?
A: Feature Store 是增量构建的，已有月份自动跳过。首次全量构建耗时较长 (~1h/策略)，后续增量只需几分钟。

### Q: ADOPT 后如何回退?
A: `config/strategies/` 在 git 中，`git revert` 即可回退。

### Q: DEPLOY 后如何回退?
A: `python scripts/deploy_config_to_live.py --rollback` 查看指引，或直接 `git revert HEAD`。

### Q: 如何只重跑某一步?
A: 参考第六节手动分步执行，找到对应 Step 的命令单独运行。

### Q: ME 的 timeframe 是什么?
A: ME 使用 60T (1小时), BPC/FER 使用 240T (4小时)。pipeline 配置中已自动区分。

### 向量回测 vs 事件回测对比

**向量回测命令 (Vector Backtest):**
```bash
# 单策略向量回测
python scripts/backtest_execution_layer.py --logs results/train_final_*/bpc/logs_gated.parquet --strategy bpc

# 多策略 PCM 联合回测 (推荐)
cd /home/yin/trading/ml_trading_bot && python scripts/backtest_execution_layer.py \
  --pcm bpc:results/train_final_*/bpc/logs_gated.parquet \
       fer:results/train_final_*/fer/logs_gated.parquet \
       me:results/train_final_*/me/logs_gated.parquet \
  --strategies-root config/strategies \
  --export-trades /tmp/vector_trades.csv
```

**事件回测命令 (Event Backtest):**
```bash
# 单策略事件回测
python scripts/event_backtest.py --strategy bpc --days 180

# 多策略事件回测 (推荐)
python scripts/event_backtest.py --strategy bpc,fer,me --days 180 --export /tmp/event_trades.csv
```

**比较结果命令:**
```bash
# 对比向量回测和事件回测结果
python scripts/compare_vector_event_consistency.py --vector /tmp/vector_trades.csv --event /tmp/event_trades.csv
```

**说明:**
- 向量回测 (Vector Backtest) 使用批量处理方式，速度快但时序精度较低
- 事件回测 (Event Backtest) 模拟实时逐tick处理，更接近实盘表现
- 两者应该产生相似的交易数、胜率、夏普比率等关键指标
- 如果差异过大，需要检查 gate 规则、特征计算或其他配置一致性问题

**回测一致性对齐进展 (2026-03-01)**:

| 层 | 状态 | 说明 |
|----|------|------|
| 特征计算 | ✅ 已对齐 | 36/36 特征 >=99.9% 匹配 |
| gate 评估 | ✅ 已对齐 | 向量化 gate 支持 all_of/any_of + AND 语义，`--from-raw --recompute-gate` 可重算 |
| entry_filter | ✅ 已对齐 | 事件侧确认包含 entry_filter |
| pre-slot 信号数 | ✅ 已对齐 | 1895 (向量) ≈ 1879 (事件) |
| slot 行为 | 🔧 修复中 | 向量改为 1min exit_ts_ns 精度，待验证 |

**已修复 Bug 清单 (回测一致性)**:
1. gate 向量化不支持 `all_of`/`any_of` 谓词 → FER prefilter 被跳过
2. 多 feature when 条件用 OR 而非 AND → gate 通过率虚高
3. `per_strategy_limits` 耦合于 `add_position_cfg` → slot 分配不正确
4. 事件回测漏斗 `lf.get("gate", True)` 默认值错误 → reject 分类不准
5. PCM 跳过 timeframe 不匹配策略时 `_last_funnel` 保留陈旧数据 → 漏斗统计偏差
6. 向量回测 slot 判定用 bar 级 exit_idx (4H 粒度) → 改为 1min exit_ts_ns 时间戳

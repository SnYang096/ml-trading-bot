
# 每个archetype分为long和short配置，可以充分发现不同方向的特征配置，也可以关闭一个方向的交易，从而增加稳定性
# 原来的方向（direction）还是保留，作为一种过滤，比如做多的配置，如果direction显示空，则不能开仓，一种提高置信度的方法
# prefilter应该主要使用>0.8r作为训练目标，找到好的数据分布
# gate使用<0.8r作为主要训练目标，过滤差的数据分布
# entry filter在gate和prefilter过滤后的数据上 特征×阈值穷举搜索 snotio 优化 mean(R-multiples) （基于分位数 + plateau 稳定性）
# evidence labels_return_tree evidence_score 列（概率分数），由于训练连续概率，可以把测试数据从oos扩大到测试集合（这个如果无效也可以跳过）

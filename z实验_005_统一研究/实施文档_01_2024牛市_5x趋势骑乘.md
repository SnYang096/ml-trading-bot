# 实施文档 01：2024 牛市 5x 趋势骑乘（精简版）

## 1. 目标

- 在 2024 牛市窗口验证 `bpc-long-120T`、`me-long-120T` 是否能稳定接近 `5x` 杠杆。
- 验证“先保本再加仓”的风险约束仍成立。
- 验证收益主要来自趋势段，而不是短噪音。

## 2. 两种实验模式（只看这个）

### 快模式，只调整阈值
```bash

# 先跑单月快检（看阈值和图是否修复）：
mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage fast_month --month 2024-09 --max-slots 3 --skip-shap

mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage rolling_sim --skip-shap
```

### 如果只调试执行层，就直接跑事件回测 
```bash
# 方案 A：走 pipeline run（最省事）
mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage event_backtest --skip-shap

# 方案 B：基于已有实验重放事件回测（不重训，更快）

# 分别跑 long/short：

mlbot pipeline run --all --config config/prod_train_pipeline_2h_turbo_2024bull_thresholds_only_bpc_only.yaml --stage fast_month --month 2024-09 --max-slots 6 --skip-shap

# 注意：
# - fast_month 已内置 event_backtest，会输出 event_trades/trading_map。
# - 不要在 fast_month 结果上再调用 `mlbot pipeline event-backtest`（它只认 research_history/<strategy>/<hash> 目录）。

# 如果要 1min 精细模式，把 --fast 去掉（或改 --no-fast）。
```

### 发现fs出现问题，先删除再重建
layer = "<你的当前layer>"
layer = "features_bpc-long-120T_120T_c2bdc21bd1"
layer = "features_bpc-short-120T_120T_81b6837b97"
```bash
python - <<'PY'
from src.feature_store.feature_store import FeatureStore, FeatureStoreSpec

root = "feature_store"
layer = "features_bpc-short-120T_120T_81b6837b97"
timeframe = "120T"
month = "2024-09"
symbols = ["BTCUSDT","ETHUSDT","BNBUSDT","SOLUSDT","XRPUSDT","ADAUSDT"]

drop_cols = {
    "fp_poc","fp_vah","fp_val","fp_hvn","fp_lvn","fp_max_imbalance_price","fp_exhaustion_price"
}
base_cols = ["open","high","low","close","volume","_symbol","symbol"]

store = FeatureStore(root)
for sym in symbols:
    spec = FeatureStoreSpec(layer=layer, symbol=sym, timeframe=timeframe)
    df = store.read_month(spec, month)
    meta = store.read_month_meta(spec, month)
    keep = [c for c in df.columns if c not in drop_cols]
    df2 = df[keep].copy()
    feat_cols = [c for c in df2.columns if c not in base_cols]
    store.write_month(
        spec, month, df2,
        base_columns=base_cols,
        feature_columns=feat_cols,
        overwrite=True,
        metadata=(meta.get("metadata", {}) if isinstance(meta, dict) else {}),
    )
    print("patched", sym, month)
PY

mlbot feature-store build \
  --config config/strategies/bpc-short-120T \
  --symbols BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT,ADAUSDT \
  --timeframe 120T \
  --start-date 2024-09-01 \
  --end-date 2024-09-30 \
  --root feature_store \
  --layer features_bpc-short-120T_120T_81b6837b97
```
